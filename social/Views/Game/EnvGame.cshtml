
@{
    ViewData["Title"] = "EnvGame";
    Layout = "~/Views/Shared/_LayoutAc.cshtml";
}



<style>

    .euser {
        position: absolute;
        height: 593px;
        width: 24%;
        background: black;
        top: 50px;
        right: 0;
        float: right;
    }

    .emain {
        background:black;
        position: absolute;
        height: 643px;
        width: 76%;
        float: right;
        right: 24%;
        border: 4px solid white;
        overflow: hidden;
    }

    .etable {
        width: 800px;
        height: 560px;
        position: absolute;
        top: 80px;
        right: 60px;
    }

    .echob {
        position: absolute;
        height: 570px;
        width: 700px;
        z-index: 10; 
        top:-265px;
        left:420px;
        transform: rotate(294deg);
    }
    #dchob {
        position: relative;
        height: 30px;
        width: 30px;
        z-index: 10;
        cursor: move;
    @*   transform: rotate(215deg);*@

    }
    .bb {
        position: relative;
        transform: rotate(-90deg);
        height: 30px;
        width: 30px;
    }

    .ewhite {
        position: relative;
        height: 30px;
        width: 30px;

    }

    .eeight {
        position: relative;
        height: 30px;
        width: 30px;
        transform: rotate(-90deg);
    }
 
    .ebal1 {
        position: relative;
        height: 30px;
        width: 30px;
        transform: rotate(-90deg);
    }

   /* .ebal2 {
        position: relative;
        height: 30px;
        width: 30px;
        transform: rotate(-90deg);
    }

    .ebal3 {
        position: relative;
        height: 30px;
        width: 30px;
        transform: rotate(-90deg);
    }

    .ebal4 {
        position: relative;
        height: 30px;
        width: 30px;
        transform: rotate(-90deg);
    }

    .ebal5 {
        position: relative;
        height: 30px;
        width: 30px;
        transform: rotate(-90deg);
    }

    .ebal6 {
        position: relative;
        height: 30px;
        width: 30px;
        transform: rotate(-90deg);
    }

    .ebal7 {
        position: relative;
        height: 30px;
        width: 30px;
        transform: rotate(-90deg);
    }

    .ebal9 {
        position: relative;
        height: 30px;
        width: 30px;
        transform: rotate(-90deg);
    }

    .ebal10 {
        position: relative;
        height: 30px;
        width: 30px;
        transform: rotate(-90deg);
    }

    .ebal11 {
        position: relative;
        height: 30px;
        width: 30px;
        transform: rotate(-90deg);
    }

    .ebal12 {
        position: relative;
        height: 30px;
        width: 30px;
        transform: rotate(-90deg);
    }

    .ebal13 {
        position: relative;
        height: 30px;
        width: 30px;
        transform: rotate(-90deg);
    }

    .ebal14 {
        position: relative;
        height: 30px;
        width: 30px;
        transform: rotate(-90deg);
    }

    .ebal15 {
        position: relative;
        height: 30px;
        width: 30px;
        transform: rotate(-90deg);
    }
*/
    .detable {
        width: 720px;
        height: 340px;
        position: absolute;
        top: 190px;
        right: 97px;
        background: red;
        z-index: 1;
        border-radius: 50px;
        opacity: .5;
        overflow: hidden;
    }

    .liner {
        height: 30px;
        background: white;
        position: relative;
        width: 1000px;
    }
    .wall0 {
        width: 20px;
        background-color: blue;
        position: absolute;
    }
    .wall {
        height: 20px;
        background-color: blue;
        position: absolute;
    }
    .wall1 {
        top: 30px;
        left: -18px;
        height: 280px;
    }
    .wall2 {
        top: -18px;
        left: 40px;
        width: 294px;
    }
    .wall3 {
        top: -18px;
        left: 386px;
        width: 294px;
    }
    .wall4 {
        top: 338px;
        left: 40px;
        width: 294px;
    }
    .wall5 {
        top: 338px;
        left: 386px;
        width: 294px;
    }
    .wall6 {
        top: 30px;
        right: -18px;
        height: 280px;
    }

    .hole {
        width: 40px;
        height: 40px;
        background-color: gray;
        border-radius: 30%;
        position: absolute;

    }
    .hole1 {
        top: -13px;
        right: -10px;
    }
    .hole2 {
        bottom: -13px;
        right: -10px;
    }
    .hole3 {
        top: -32px;
        right: 340px;
    }
    .hole4 {
        bottom: -32px;
        right: 340px;
    }
    .hole5 {
        top: -16px;
        right: 694px;
    }
    .hole6 {
        bottom: -16px;
        right: 694px;
    }
        .hole:nth-child(1) {
            top: 0;
            left: 0;
        }

        .hole:nth-child(2) {
            top: 0;
            left: 340px;
        }

        .hole:nth-child(3) {
            top: 0;
            right: 0;
        }

        .hole:nth-child(4) {
            bottom: 0;
            left: 0;
        }

        .hole:nth-child(5) {
            bottom: 0;
            left: 340px;
        }

        .hole:nth-child(6) {
            bottom: 0;
            right: 0;
        }
    /* این قسمت برای طراحی کنترل کنترل افزایش و کاهش است */
    #container {
        position: absolute; /* موقعیت کنترل کنترل را نسبت به جای خود تعیین می کند */
        top: 50%; /* فاصله کنترل کنترل را از بالای صفحه تعیین می کند */
        left: 50%; /* فاصله کنترل کنترل را از چپ صفحه تعیین می کند */
        transform: translate(-50%, -50%); /* کنترل کنترل را در مرکز صفحه قرار می دهد */
    }

    #value {
        width: 100px; /* عرض input را تعیین می کند */
        height: 30px; /* ارتفاع input را تعیین می کند */
        font-size: 20px; /* اندازه فونت input را تعیین می کند */
        text-align: center; /* متن input را در وسط قرار می دهد */
    }

    .button {
        width: 30px; /* عرض دکمه ها را تعیین می کند */
        height: 30px; /* ارتفاع دکمه ها را تعیین می کند */
        font-size: 20px; /* اندازه فونت دکمه ها را تعیین می کند */
        background-color: green; /* رنگ دکمه ها را سبز می کند */
        color: white; /* رنگ متن دکمه ها را سفید می کند */
        border: none; /* حاشیه دکمه ها را حذف می کند */
        cursor: pointer; /* شکل موس را به دستی تغییر می دهد */
    }

    #plus {
        border-top-left-radius: 15px; /* شکل دکمه افزایش را گرد می کند */
        border-bottom-left-radius: 15px; /* شکل دکمه افزایش را گرد می کند */
    }

    #minus {
        border-top-right-radius: 15px; /* شکل دکمه کاهش را گرد می کند */
        border-bottom-right-radius: 15px; /* شکل دکمه کاهش را گرد می کند */
    }
</style>
<div class="euser">
    <div id="container">
        <!-- این قسمت برای ایجاد یک کنترل کنترل افزایش و کاهش در صفحه است -->
       <button id="plus" class="button" onclick="increase()">+</button>--> <!-- این قسمت برای ایجاد یک دکمه افزایش است که با کلیک کردن روی آن، تابع increase را اجرا می کند -->
      <input id="value" type="text" value="0">--> <!-- این قسمت برای ایجاد یک input است که مقدار را نشان می دهد -->
     <button id="minus" class="button" onclick="decrease()">-</button>--> <!-- این قسمت برای ایجاد یک دکمه کاهش است که با کلیک کردن روی آن، تابع decrease را اجرا می کند -->
     </div>
    </div>
        <div class="emain">

            <div class="detable" id="detable">

                <img src="./picture/biliard/eight.png" class="eeight bbt" id="eeight" />

                <img src="./picture/biliard/b1.png" class="ebal1 bb" id="eball1" />
                @*<img src="./picture/biliard/b2.png" class="ebal2 bb" id="eball2" />
                <img src="./picture/biliard/b3.png" class="ebal3 bb" id="eball3" />
                <img src="./picture/biliard/b4.png" class="ebal4 bb" id="eball4" />
                <img src="./picture/biliard/b5.png" class="ebal5 bb" id="eball5" />
                <img src="./picture/biliard/b6.png" class="ebal6 bb" id="eball6" />
                <img src="./picture/biliard/b7.png" class="ebal7 bb" id="eball7" />
                <img src="./picture/biliard/b9.png" class="ebal9 bb" id="eball9" />
                <img src="./picture/biliard/b10.png" class="ebal10 bb" id="eball10" />
                <img src="./picture/biliard/b11.png" class="ebal11 bb" id="eball11" />
                <img src="./picture/biliard/b12.png" class="ebal12 bb" id="eball12" />
                <img src="./picture/biliard/b13.png" class="ebal13 bb" id="eball13" />
                <img src="./picture/biliard/b14.png" class="ebal14 bb" id="eball14" />
                <img src="./picture/biliard/b15.png" class="ebal15 bb" id="eball15" />*@



                <div class="wall0 wall1" id="wall1"></div>
                <div class="wall wall2" id="wall2"></div>
                <div class="wall wall3" id="wall3"></div>
                <div class="wall wall4" id="wall4"></div>
                <div class="wall wall5" id="wall5"></div>
                <div class="wall0 wall6" id="wall6"></div>
                <div class="hole hole1"></div>
                <div class="hole hole2"></div>
                <div class="hole hole3"></div>
                <div class="hole hole4"></div>
                <div class="hole hole5"></div>
                <div class="hole hole6"></div>
                <div class="liner"></div>
            </div>
            <div id="dchob">
                <img src="./picture/biliard/white.png" class="ewhite bb" id="ewhite" />
                <img src="./picture/biliard/chob.png" class="echob" id="echob" />
            </div>
            <img src="./picture/biliard/table.png" class="etable" />
            <div style="background:white;" id="GFG"></div>

        </div>
    
       @*$('.detable').mousemove(function(event) {
         $('.liner').resizable().rotatable().draggable();

        $(".liner").position({
          my: "left+3 bottom-3",
          of: event,
          collision: "fit"
         });
         });*@
    <script>

        var element = document.getElementById('dchob');
        let isMouseDown = false;
        let initialX;
        let initialY;
        let angle, angle1, angle2, angle3, angle4, angle5, angle6, angle7, angle8, angle9, angle10, angle11, angle12, angle13, angle14, angle15;
        var speed = 20, speed1, speed2, speed3, speed4, speed5, speed6, speed7, speed8, speed9, speed10, speed11, speed12, speed13, speed14, speed15;
        var bwhite = document.getElementById("dchob");

        var wall1 = document.getElementById("wall1");
        var wall2 = document.getElementById("wall2");
        var wall3 = document.getElementById("wall3");
        var wall4 = document.getElementById("wall4");
        var wall5 = document.getElementById("wall5");
        var wall6 = document.getElementById("wall6");

        var eball1 = document.getElementById("eball1");
        //var eball2 = document.getElementById("eball2");
        //var eball3 = document.getElementById("eball3");
        //var eball4 = document.getElementById("eball4");
        //var eball5 = document.getElementById("eball5");
        //var eball6 = document.getElementById("eball6");
        //var eball7 = document.getElementById("eball7");
        //var eball9 = document.getElementById("eball9");
        //var eball10 = document.getElementById("eball10");
        //var eball11 = document.getElementById("eball11");
        //var eball12 = document.getElementById("eball12");
        //var eball13 = document.getElementById("eball13");
        //var eball14 = document.getElementById("eball14");
        //var eball15 = document.getElementById("eball15");
        var eeight = document.getElementById("eeight");
        // سرعت حرکت توپ

        function increase() { // این قسمت برای تعریف یک تابع برای افزایش مقدار است
            var value = document.getElementById("value"); // این قسمت برای گرفتن input است
            var number = parseInt(value.value); // این قسمت برای تبدیل مقدار input به عدد است
            number++; // این قسمت برای افزایش عدد یک واحد است
            value.value = number; // این قسمت برای نمایش عدد جدید در input است
        }
        function decrease() { // این قسمت برای تعریف یک تابع برای کاهش مقدار است
            var value = document.getElementById("value"); // این قسمت برای گرفتن input است
            var number = parseInt(value.value); // این قسمت برای تبدیل مقدار input به عدد است
            number--; // این قسمت برای کاهش عدد یک واحد است
            value.value = number; // این قسمت برای نمایش عدد جدید در input است
        }
        //$("#echob").click(function () {
        //    $("#echob").css({ 'animation': 'moveWood 4s alternate' });
        //})
        var X,Y;
        $(document).ready(function () {




                $(".detable").on("mousemove", function (event) {
                    $("#GFG").text("Mouse Position ("
                    + event.pageX + ", " + event.pageY + ")");
        X=event.pageX;
        Y=event.pageY;

                });
        });


        //var x2 = $("#dchob").position().left; // این قسمت برای گرفتن موقعیت افقی توپ قرمز است
        //var y2 = $("#dchob").position().top; // این قسمت برای گرفتن موقعیت عمودی توپ قرمز است

        //x2 = x2-760 ;
        //y2 = y2 + 330;

        //$("#detable").click(function () {
        //    //$("#dchob").css({ left: x2, top: y2 });



        //})
        ballstart();

        document.addEventListener("mousedown", function (e) {



            $("#echob").show();

                isMouseDown = true;
            //initialX = e.clientX;
            //initialY = e.clientY;
            initialX = X;
            initialY = Y;
        });
        //var vy = Math.sin(angle) * speed
        document.addEventListener("mouseup", function (e) {
            //var bwhite = $('#dchob');
            //var wall = $('.wall');
            //var wall = document.getElementsByClassName("wall");
            //var wall1 = $('.wall1');
            //var wall2 = $('.wall2');
            //var wall3 = $('.wall3');
            //var wall4 = $('.wall4');
            //var wall5 = $('.wall5');
            //var wall6 = $('.wall6');


            isMouseDown = false;

            //     // مشخصات اولیه توپ
            //     //var ballPosition = { top: 50, left: 50 };
            //     //var angle = 45; // زاویه حرکت توپ
            ///*     console.log(angle);*/

            //     // تنظیم موقعیت اولیه توپ
            //     //ball.css(ballPosition);

            //     // حرکت توپ با زاویه
            //     function moveBall() {
            // فراخوانی تابع حرکت توپ


            //         // محاسبه تغییرات موقعیت بر اساس زاویه
                     //var deltaX = speed * Math.cos(angle * Math.PI / 180);
                     //var deltaY = speed * Math.sin(angle * Math.PI / 180);
                     ////var deltaX = speed * (angle);
                     ////var deltaY = speed * (angle);

                   /*  // به‌روزرسانی موقعیت توپ*/
                     //bwhite.css({
                     //    top: '+=' + deltaY,
                     //    left: '+=' + deltaX
                     //});
            moveBall();

            function moveBall() {

                // محاسبه تغییرات موقعیت بر اساس زاویه
                var deltaX = speed * Math.cos(angle * Math.PI / 180);
                var deltaY = speed * Math.sin(angle * Math.PI / 180);

                var deltaX1 = speed1 * Math.cos(angle1 * Math.PI / 180);
                var deltaY1 = speed1 * Math.sin(angle1 * Math.PI / 180);

                var deltaX2 = speed2 * Math.cos(angle2 * Math.PI / 180);
                var deltaY2 = speed2 * Math.sin(angle2 * Math.PI / 180);

                var deltaX3 = speed3 * Math.cos(angle3 * Math.PI / 180);
                var deltaY3 = speed3 * Math.sin(angle3 * Math.PI / 180);

                var deltaX4 = speed4 * Math.cos(angle4 * Math.PI / 180);
                var deltaY4 = speed4 * Math.sin(angle4 * Math.PI / 180);

                var deltaX5 = speed5 * Math.cos(angle5 * Math.PI / 180);
                var deltaY5 = speed5 * Math.sin(angle5 * Math.PI / 180);

                var deltaX6 = speed6 * Math.cos(angle6 * Math.PI / 180);
                var deltaY6 = speed6 * Math.sin(angle6 * Math.PI / 180);

                var deltaX7 = speed7 * Math.cos(angle7 * Math.PI / 180);
                var deltaY7 = speed7 * Math.sin(angle7 * Math.PI / 180);

                var deltaX8 = speed8 * Math.cos(angle8 * Math.PI / 180);
                var deltaY8 = speed8 * Math.sin(angle8 * Math.PI / 180);

                var deltaX9 = speed9 * Math.cos(angle9 * Math.PI / 180);
                var deltaY9 = speed9 * Math.sin(angle9 * Math.PI / 180);

                var deltaX10 = speed10 * Math.cos(angle10 * Math.PI / 180);
                var deltaY10 = speed10 * Math.sin(angle10 * Math.PI / 180);

                var deltaX11 = speed11 * Math.cos(angle11 * Math.PI / 180);
                var deltaY11 = speed11 * Math.sin(angle11 * Math.PI / 180);

                var deltaX12 = speed12 * Math.cos(angle12 * Math.PI / 180);
                var deltaY12 = speed12 * Math.sin(angle12 * Math.PI / 180);

                var deltaX13 = speed13 * Math.cos(angle13 * Math.PI / 180);
                var deltaY13 = speed13 * Math.sin(angle13 * Math.PI / 180);

                var deltaX14 = speed14 * Math.cos(angle14 * Math.PI / 180);
                var deltaY14 = speed14 * Math.sin(angle14 * Math.PI / 180);

                var deltaX15= speed15 * Math.cos(angle15 * Math.PI / 180);
                var deltaY15 = speed15 * Math.sin(angle15 * Math.PI / 180);
                // به‌روزرسانی موقعیت توپ
                // به‌روزرسانی موقعیت توپ
            bwhite.style.left = (parseFloat(bwhite.style.left) + deltaX) + "px";
                bwhite.style.top = (parseFloat(bwhite.style.top) + deltaY) + "px";

                eball1.style.left = (parseFloat(eball1.style.left) + deltaX1) + "px";
                eball1.style.top = (parseFloat(eball1.style.top) + deltaY1) + "px";

                //eball2.style.left = (parseFloat(eball2.style.left) + deltaX2) + "px";
                //eball2.style.top = (parseFloat(eball2.style.top) + deltaY2) + "px";

                //eball3.style.left = (parseFloat(eball3.style.left) + deltaX3) + "px";
                //eball3.style.top = (parseFloat(eball3.style.top) + deltaY3) + "px";

                //eball4.style.left = (parseFloat(eball4.style.left) + deltaX4) + "px";
                //eball4.style.top = (parseFloat(eball4.style.top) + deltaY4) + "px";

                //eball5.style.left = (parseFloat(eball5.style.left) + deltaX5) + "px";
                //eball5.style.top = (parseFloat(eball5.style.top) + deltaY5) + "px";

                //eball6.style.left = (parseFloat(eball6.style.left) + deltaX6) + "px";
                //eball6.style.top = (parseFloat(eball6.style.top) + deltaY6) + "px";

                //eball7.style.left = (parseFloat(eball7.style.left) + deltaX7) + "px";
                //eball7.style.top = (parseFloat(eball7.style.top) + deltaY7) + "px";

                //eball9.style.left = (parseFloat(eball9.style.left) + deltaX9) + "px";
                //eball9.style.top = (parseFloat(eball9.style.top) + deltaY9) + "px";

                //eball10.style.left = (parseFloat(eball10.style.left) + deltaX10) + "px";
                //eball10.style.top = (parseFloat(eball10.style.top) + deltaY10) + "px";

                //eball11.style.left = (parseFloat(eball11.style.left) + deltaX11) + "px";
                //eball11.style.top = (parseFloat(eball11.style.top) + deltaY11) + "px";

                //eball12.style.left = (parseFloat(eball12.style.left) + deltaX12) + "px";
                //eball12.style.top = (parseFloat(eball12.style.top) + deltaY12) + "px";

                //eball13.style.left = (parseFloat(eball13.style.left) + deltaX13) + "px";
                //eball13.style.top = (parseFloat(eball13.style.top) + deltaY13) + "px";

                //eball14.style.left = (parseFloat(eball14.style.left) + deltaX14) + "px";
                //eball14.style.top = (parseFloat(eball14.style.top) + deltaY14) + "px";

                //eball15.style.left = (parseFloat(eball15.style.left) + deltaX15) + "px";
                //eball15.style.top = (parseFloat(eball15.style.top) + deltaY15) + "px";

                eeight.style.left = (parseFloat(eeight.style.left) + deltaX8) + "px";
                eeight.style.top = (parseFloat(eeight.style.top) + deltaY8) + "px";
                // فراخوانی تابع حرکت توپ با تاخیر
                requestAnimationFrame(moveBall);
                // بررسی برخورد با خط‌ها
                checkCollision(bwhite, eball1, eeight, wall1, wall2, wall3, wall4, wall5, wall6);

                //checkCollision(bwhite, eball1, eball2, eball3, eball4, eball5, eball6, eball7, eball9, eball10, eball11, eball12, eball13, eball14, eball15, eeight, wall1, wall2, wall3, wall4, wall5, wall6);
             //   checkCollision2(bwhite, eball1, eball2, eball3, eball4, eball5, eball6, eball7, eball9, eball10, eball11, eball12, eball13, eball14, eball15, eeight, wall1, wall2, wall3, wall4, wall5, wall6);




            }

            ///////////////////////////////////////////////////////////////////////////////
          //  function checkCollision0(ball, eball1, eball2, eball3, eball4, eball5, eball6, eball7, eball9, eball10, eball11, eball12, eball13, eball14, eball15, eeight, wall11, wall22, wall33, wall44, wall55, wall66) {

          //      var ballRect = ball.getBoundingClientRect();
          //      var ballRect1 = eball1.getBoundingClientRect();
          //var ballRect2 = eball2.getBoundingClientRect();
          //      var ballRect3 = eball3.getBoundingClientRect();
          //      var ballRect4 = eball4.getBoundingClientRect();
          //      var ballRect5 = eball5.getBoundingClientRect();
          //      var ballRect6 = eball6.getBoundingClientRect();
          //      var ballRect7 = eball7.getBoundingClientRect();

          //      var ballRect9 = eball9.getBoundingClientRect();
          //      var ballRect10 = eball10.getBoundingClientRect();
          //      var ballRect11 = eball11.getBoundingClientRect();
          //      var ballRect12 = eball12.getBoundingClientRect();
          //      var ballRect13 = eball13.getBoundingClientRect();
          //      var ballRect14 = eball14.getBoundingClientRect();
          //      var ballRect15 = eball15.getBoundingClientRect();
          //      var eightRect = eeight.getBoundingClientRect();

          //      var wallRect1 = wall11.getBoundingClientRect();
          //      var wallRect2 = wall22.getBoundingClientRect();
          //      var wallRect3 = wall33.getBoundingClientRect();
          //      var wallRect4 = wall44.getBoundingClientRect();
          //      var wallRect5 = wall55.getBoundingClientRect();
          //      var wallRect6 = wall66.getBoundingClientRect();



          //      $("#echob").hide();



          //      if (ballRect.right >= wallRect1.left &&
          //          ballRect.left <= wallRect1.right &&
          //          ballRect.bottom >= wallRect1.top &&
          //          ballRect.top <= wallRect1.bottom) {
          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ
          //          angle = 200 - angle;
          //          if (speed > 0) {

          //              speed -= 1;

          //          }
          //      }

          //      if (ballRect.right >= wallRect2.left &&
          //          ballRect.left <= wallRect2.right &&
          //          ballRect.bottom >= wallRect2.top &&
          //          ballRect.top <= wallRect2.bottom) {

          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ
          //          angle = 250 + angle;
          //          if (speed > 0) {

          //              speed -= 1;

          //          }

          //      }

          //      if (ballRect.right >= wallRect3.left &&
          //          ballRect.left <= wallRect3.right &&
          //          ballRect.bottom >= wallRect3.top &&
          //          ballRect.top <= wallRect3.bottom) {

          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ
          //          angle = 250 + angle;
          //          if (speed > 0) {

          //              speed -= 1;

          //          }

          //      }

          //      if (ballRect.right >= wallRect4.left &&
          //          ballRect.left <= wallRect4.right &&
          //          ballRect.bottom >= wallRect4.top &&
          //          ballRect.top <= wallRect4.bottom) {

          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ
          //          angle = 250 + angle;
          //          if (speed > 0) {

          //              speed -= 1;

          //          }

          //      }

          //      if (ballRect.right >= wallRect5.left &&
          //          ballRect.left <= wallRect5.right &&
          //          ballRect.bottom >= wallRect5.top &&
          //          ballRect.top <= wallRect5.bottom) {

          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ

          //          angle = 250 + angle;

          //          if (speed > 0) {

          //              speed -= 1;

          //          }
          //      }

          //      if (ballRect.right >= wallRect6.left &&
          //          ballRect.left <= wallRect6.right &&
          //          ballRect.bottom >= wallRect6.top &&
          //          ballRect.top <= wallRect6.bottom) {

          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ
          //          angle = 200 - angle;

          //          if (speed > 0) {

          //              speed -= 1;

          //          }
          //      }
          //      //////////////////////////////////////////////////////////b1
          //      if (ballRect1.right >= ballRect.left &&
          //          ballRect1.left <= ballRect.right &&
          //          ballRect1.bottom >= ballRect.top &&
          //          ballRect1.top <= ballRect.bottom) {
          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ

          //          angle1 = (angle + 180) % 360;

          //          if (speed1 > 0) {
          //              speed1 = speed - 1;


          //          }
          //          console.log("angle1="+angle);

          //      }

          //      if (ballRect1.right >= wallRect1.left &&
          //          ballRect1.left <= wallRect1.right &&
          //          ballRect1.bottom >= wallRect1.top &&
          //          ballRect1.top <= wallRect1.bottom) {
          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ
          //          angle1 = 200 - angle1;
          //          if (speed1 > 0) {

          //              speed1 = speed1 - 1;


          //          }

          //      }

          //      if (ballRect1.right >= wallRect2.left &&
          //          ballRect1.left <= wallRect2.right &&
          //          ballRect1.bottom >= wallRect2.top &&
          //          ballRect1.top <= wallRect2.bottom) {

          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ
          //          angle1 = 250 + angle;

          //          if (speed1 > 0) {

          //              speed1 = speed1 - 1;


          //          }
          //      }

          //      if (ballRect1.right >= wallRect3.left &&
          //          ballRect1.left <= wallRect3.right &&
          //          ballRect1.bottom >= wallRect3.top &&
          //          ballRect1.top <= wallRect3.bottom) {

          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ
          //          angle1 = 250 + angle1;

          //          if (speed1 > 0) {

          //              speed1 = speed1 - 1;


          //          }
          //      }

          //      if (ballRect1.right >= wallRect4.left &&
          //          ballRect1.left <= wallRect4.right &&
          //          ballRect1.bottom >= wallRect4.top &&
          //          ballRect1.top <= wallRect4.bottom) {

          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ
          //          angle1 = 250 + angle1;

          //          if (speed1 > 0) {

          //              speed1 = speed1 - 1;


          //          }
          //      }

          //      if (ballRect1.right >= wallRect5.left &&
          //          ballRect1.left <= wallRect5.right &&
          //          ballRect1.bottom >= wallRect5.top &&
          //          ballRect1.top <= wallRect5.bottom) {

          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ

          //          angle1 = 250 + angle1;

          //          if (speed1 > 0) {

          //              speed1 =speed1- 1;

          //          }
          //      }

          //      if (ballRect1.right >= wallRect6.left &&
          //          ballRect1.left <= wallRect6.right &&
          //          ballRect1.bottom >= wallRect6.top &&
          //          ballRect1.top <= wallRect6.bottom) {

          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ
          //          angle1 = 200 - angle1;

          //          if (speed1 > 0) {

          //              speed1 -= 1;

          //          }
          //      }
          //      /////////////////////////////////////////////////////////////////////////////////b2
          //      if (ballRect2.right >= ballRect.left &&
          //          ballRect2.left <= ballRect.right &&
          //          ballRect2.bottom >= ballRect.top &&
          //          ballRect2.top <= ballRect.bottom) {
          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ

          //          angle2 = (angle + 180) % 360;


          //          if (speed2 > 0) {

          //              speed2 = speed - 1;


          //          }
          //          console.log("angle2=" + angle);


          //      }

          //      if (ballRect2.right >= ballRect1.left &&
          //          ballRect2.left <= ballRect1.right &&
          //          ballRect2.bottom >= ballRect1.top &&
          //          ballRect2.top <= ballRect1.bottom) {
          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ

          //          angle2 = (angle1 + 180) % 360;


          //          if (speed2 > 0) {
          //              speed2 = speed2 - 1;


          //          }

          //      }

          //      if (ballRect2.right >= wallRect1.left &&
          //          ballRect2.left <= wallRect1.right &&
          //          ballRect2.bottom >= wallRect1.top &&
          //          ballRect2.top <= wallRect1.bottom) {
          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ
          //          angle2 = 200 - angle;
          //          if (speed2 > 0) {

          //              speed2 = speed2 - 1;


          //          }

          //      }

          //      if (ballRect2.right >= wallRect2.left &&
          //          ballRect2.left <= wallRect2.right &&
          //          ballRect2.bottom >= wallRect2.top &&
          //          ballRect2.top <= wallRect2.bottom) {

          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ
          //          angle2 = 250 + angle2;
          //          if (speed2 > 0) {

          //              speed2 = speed2 - 1;


          //          }

          //      }

          //      if (ballRect2.right >= wallRect3.left &&
          //          ballRect2.left <= wallRect3.right &&
          //          ballRect2.bottom >= wallRect3.top &&
          //          ballRect2.top <= wallRect3.bottom) {

          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ
          //          angle2 = 250 + angle2;
          //          if (speed2 > 0) {

          //              speed2 = speed2- 1;


          //          }

          //      }

          //      if (ballRect2.right >= wallRect4.left &&
          //          ballRect2.left <= wallRect4.right &&
          //          ballRect2.bottom >= wallRect4.top &&
          //          ballRect2.top <= wallRect4.bottom) {

          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ
          //          angle2 = 250 + angle2;
          //          if (speed2 > 0) {
          //              speed2 = speed2 - 1;


          //          }

          //      }

          //      if (ballRect2.right >= wallRect5.left &&
          //          ballRect2.left <= wallRect5.right &&
          //          ballRect2.bottom >= wallRect5.top &&
          //          ballRect2.top <= wallRect5.bottom) {

          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ

          //          angle2 = 250 + angle2;
          //          if (speed2 > 0) {

          //              speed2 = speed2 - 1;


          //          }

          //      }

          //      if (ballRect2.right >= wallRect6.left &&
          //          ballRect2.left <= wallRect6.right &&
          //          ballRect2.bottom >= wallRect6.top &&
          //          ballRect2.top <= wallRect6.bottom) {

          //          // برخورد اتفاق افتاده است، تغییر رنگ توپ
          //          angle2 = 200 - angle2;
          //          if (speed2 > 0) {

          //              speed2 = speed2 - 1;


          //          }

          //      }
          //  }


            //function checkCollision(ball, eball1, eball2, eball3, eball4, eball5, eball6, eball7, eball9, eball10, eball11, eball12, eball13, eball14, eball15, eeight, wall11, wall22, wall33, wall44, wall55, wall66) {

            ///////////////////////////////////////////////////////////////////////////////
            function checkCollision(ball, eball1, eeight, wall11, wall22, wall33, wall44, wall55, wall66) {

                var ballRect = ball.getBoundingClientRect();
                var ballRect1 = eball1.getBoundingClientRect();
                //var ballRect2 = eball2.getBoundingClientRect();
                //var ballRect3 = eball3.getBoundingClientRect();
                //var ballRect4 = eball4.getBoundingClientRect();
                //var ballRect5 = eball5.getBoundingClientRect();
                //var ballRect6 = eball6.getBoundingClientRect();
                //var ballRect7 = eball7.getBoundingClientRect();

                //var ballRect9 = eball9.getBoundingClientRect();
                //var ballRect10 = eball10.getBoundingClientRect();
                //var ballRect11 = eball11.getBoundingClientRect();
                //var ballRect12 = eball12.getBoundingClientRect();
                //var ballRect13 = eball13.getBoundingClientRect();
                //var ballRect14 = eball14.getBoundingClientRect();
                //var ballRect15 = eball15.getBoundingClientRect();
                var eightRect = eeight.getBoundingClientRect();

                var wallRect1 = wall11.getBoundingClientRect();
                var wallRect2 = wall22.getBoundingClientRect();
                var wallRect3 = wall33.getBoundingClientRect();
                var wallRect4 = wall44.getBoundingClientRect();
                var wallRect5 = wall55.getBoundingClientRect();
                var wallRect6 = wall66.getBoundingClientRect();



             /*   $("#echob").hide();*/


                if (ballRect.right >= wallRect1.left &&
                    ballRect.left <= wallRect1.right &&
                    ballRect.bottom >= wallRect1.top &&
                    ballRect.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    if (speed > 0) {

                        speed -= 1;

                    }
                }

                if (ballRect.right >= wallRect2.left &&
                    ballRect.left <= wallRect2.right &&
                    ballRect.bottom >= wallRect2.top &&
                    ballRect.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 250 + angle;
                    if (speed > 0) {

                        speed -= 1;

                    }

                }

                if (ballRect.right >= wallRect3.left &&
                    ballRect.left <= wallRect3.right &&
                    ballRect.bottom >= wallRect3.top &&
                    ballRect.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 250 + angle;
                    if (speed > 0) {

                        speed -= 1;

                    }

                }

                if (ballRect.right >= wallRect4.left &&
                    ballRect.left <= wallRect4.right &&
                    ballRect.bottom >= wallRect4.top &&
                    ballRect.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 250 + angle;
                    if (speed > 0) {

                        speed -= 1;

                    }

                }

                if (ballRect.right >= wallRect5.left &&
                    ballRect.left <= wallRect5.right &&
                    ballRect.bottom >= wallRect5.top &&
                    ballRect.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle = 250 + angle;

                    if (speed > 0) {

                        speed -= 1;

                    }
                }

                if (ballRect.right >= wallRect6.left &&
                    ballRect.left <= wallRect6.right &&
                    ballRect.bottom >= wallRect6.top &&
                    ballRect.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;

                    if (speed > 0) {

                        speed -= 1;

                    }
                }
                //////////////////////////////////////////////////////////b1
                if (ballRect1.right >= ballRect.left &&
                    ballRect1.left <= ballRect.right &&
                    ballRect1.bottom >= ballRect.top &&
                    ballRect1.top <= ballRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle1 = (angle + 180) % 360;

                    if (speed1 > 0) {
                        speed1 = speed - 1;


                    }
                    console.log("angle1=" + angle);

                }

                if (ballRect1.right >= wallRect1.left &&
                    ballRect1.left <= wallRect1.right &&
                    ballRect1.bottom >= wallRect1.top &&
                    ballRect1.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle1 = 200 - angle1;
                    if (speed1 > 0) {

                        speed1 = speed1 - 1;


                    }

                }

                if (ballRect1.right >= wallRect2.left &&
                    ballRect1.left <= wallRect2.right &&
                    ballRect1.bottom >= wallRect2.top &&
                    ballRect1.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle1 = 250 + angle;

                    if (speed1 > 0) {

                        speed1 = speed1 - 1;


                    }
                }

                if (ballRect1.right >= wallRect3.left &&
                    ballRect1.left <= wallRect3.right &&
                    ballRect1.bottom >= wallRect3.top &&
                    ballRect1.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle1 = 250 + angle1;

                    if (speed1 > 0) {

                        speed1 = speed1 - 1;


                    }
                }

                if (ballRect1.right >= wallRect4.left &&
                    ballRect1.left <= wallRect4.right &&
                    ballRect1.bottom >= wallRect4.top &&
                    ballRect1.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle1 = 250 + angle1;

                    if (speed1 > 0) {

                        speed1 = speed1 - 1;


                    }
                }

                if (ballRect1.right >= wallRect5.left &&
                    ballRect1.left <= wallRect5.right &&
                    ballRect1.bottom >= wallRect5.top &&
                    ballRect1.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle1 = 250 + angle1;

                    if (speed1 > 0) {

                        speed1 = speed1 - 1;

                    }
                }

                if (ballRect1.right >= wallRect6.left &&
                    ballRect1.left <= wallRect6.right &&
                    ballRect1.bottom >= wallRect6.top &&
                    ballRect1.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle1 = 200 - angle1;

                    if (speed1 > 0) {

                        speed1 -= 1;

                    }
                }
                /////////////////////////////////////////////////////////////////////////////////b2
                if (ballRect2.right >= ballRect.left &&
                    ballRect2.left <= ballRect.right &&
                    ballRect2.bottom >= ballRect.top &&
                    ballRect2.top <= ballRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle2 = (angle + 180) % 360;


                    if (speed2 > 0) {

                        speed2 = speed - 1;


                    }
                    console.log("angle2=" + angle);


                }

                if (ballRect2.right >= ballRect1.left &&
                    ballRect2.left <= ballRect1.right &&
                    ballRect2.bottom >= ballRect1.top &&
                    ballRect2.top <= ballRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle2 = (angle + 180) % 360;


                    if (speed2 > 0) {
                        speed2 = speed - 1;


                    }

                }

                if (ballRect2.right >= wallRect1.left &&
                    ballRect2.left <= wallRect1.right &&
                    ballRect2.bottom >= wallRect1.top &&
                    ballRect2.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle2 = 200 - angle;
                    if (speed2 > 0) {

                        speed2 = speed2 - 1;


                    }

                }

                if (ballRect2.right >= wallRect2.left &&
                    ballRect2.left <= wallRect2.right &&
                    ballRect2.bottom >= wallRect2.top &&
                    ballRect2.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle2 = 250 + angle2;
                    if (speed2 > 0) {

                        speed2 = speed2 - 1;


                    }

                }

                if (ballRect2.right >= wallRect3.left &&
                    ballRect2.left <= wallRect3.right &&
                    ballRect2.bottom >= wallRect3.top &&
                    ballRect2.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle2 = 250 + angle2;
                    if (speed2 > 0) {

                        speed2 = speed2 - 1;


                    }

                }

                if (ballRect2.right >= wallRect4.left &&
                    ballRect2.left <= wallRect4.right &&
                    ballRect2.bottom >= wallRect4.top &&
                    ballRect2.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle2 = 250 + angle2;
                    if (speed2 > 0) {
                        speed2 = speed2 - 1;


                    }

                }

                if (ballRect2.right >= wallRect5.left &&
                    ballRect2.left <= wallRect5.right &&
                    ballRect2.bottom >= wallRect5.top &&
                    ballRect2.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle2 = 250 + angle2;
                    if (speed2 > 0) {

                        speed2 = speed2 - 1;


                    }

                }

                if (ballRect2.right >= wallRect6.left &&
                    ballRect2.left <= wallRect6.right &&
                    ballRect2.bottom >= wallRect6.top &&
                    ballRect2.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle2 = 200 - angle2;
                    if (speed2 > 0) {

                        speed2 = speed2 - 1;


                    }

                }
                ///////////////////////////////////////////////////////////////////////////////b3
                if (ballRect3.right >= ballRect.left &&
                    ballRect3.left <= ballRect.right &&
                    ballRect3.bottom >= ballRect.top &&
                    ballRect3.top <= ballRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle3 = (angle + 180) % 360;

                    if (speed3 > 0) {

                        speed3 = (speed - 1);

                    }


                }
                if (ballRect3.right >= ballRect1.left &&
                    ballRect3.left <= ballRect1.right &&
                    ballRect3.bottom >= ballRect1.top &&
                    ballRect3.top <= ballRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle3 = (angle1 + 180) % 360;

                    if (speed3 > 0) {

                        speed3 = (speed- 1);


                    }

                }
                if (ballRect3.right >= ballRect2.left &&
                    ballRect3.left <= ballRect2.right &&
                    ballRect3.bottom >= ballRect2.top &&
                    ballRect3.top <= ballRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle3 = (angle2 + 180) % 360;


                    if (speed3 > 0) {

                        speed3 = (speed - 1);


                    }
                }
                if (ballRect3.right >= wallRect1.left &&
                    ballRect3.left <= wallRect1.right &&
                    ballRect3.bottom >= wallRect1.top &&
                    ballRect3.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle3 = 200 - angle3;
                    if (speed3 > 0) {

                        speed3 -= 1;

                    }

                }

                if (ballRect3.right >= wallRect2.left &&
                    ballRect3.left <= wallRect2.right &&
                    ballRect3.bottom >= wallRect2.top &&
                    ballRect3.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle3 = 250 + angle3;
                    if (speed3 > 0) {

                        speed3 -= 1;

                    }

                }

                if (ballRect3.right >= wallRect3.left &&
                    ballRect3.left <= wallRect3.right &&
                    ballRect3.bottom >= wallRect3.top &&
                    ballRect3.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle3 = 250 + angle3;
                    if (speed3 > 0) {

                        speed3 -= 1;

                    }

                }

                if (ballRect3.right >= wallRect4.left &&
                    ballRect3.left <= wallRect4.right &&
                    ballRect3.bottom >= wallRect4.top &&
                    ballRect3.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle3 = 250 + angle3;
                    if (speed3 > 0) {

                        speed3 -= 1;

                    }

                }

                if (ballRect3.right >= wallRect5.left &&
                    ballRect3.left <= wallRect5.right &&
                    ballRect3.bottom >= wallRect5.top &&
                    ballRect3.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle3 = 250 + angle3;
                    if (speed3 > 0) {

                        speed3 -= 1;

                    }

                }

                if (ballRect3.right >= wallRect6.left &&
                    ballRect3.left <= wallRect6.right &&
                    ballRect3.bottom >= wallRect6.top &&
                    ballRect3.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle3 = 200 - angle3;
                    if (speed3 > 0) {

                        speed3 -= 1;

                    }

                }

                ///////////////////////////////////////////////////////////////////////////////b4
                if (ballRect4.right >= ballRect.left &&
                    ballRect4.left <= ballRect.right &&
                    ballRect4.bottom >= ballRect.top &&
                    ballRect4.top <= ballRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle4 = (angle + 180) % 360;



                    if (speed4 > 0) {

                        speed4 = (speed -1);

                    }
                }
                if (ballRect4.right >= ballRect1.left &&
                    ballRect4.left <= ballRect1.right &&
                    ballRect4.bottom >= ballRect1.top &&
                    ballRect4.top <= ballRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ


                    angle4 = (angle + 180) % 360;


                    if (speed4 > 0) {

                        speed4 = (speed -1);

                    }

                }
                if (ballRect4.right >= ballRect2.left &&
                    ballRect4.left <= ballRect2.right &&
                    ballRect4.bottom >= ballRect2.top &&
                    ballRect4.top <= ballRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ


                    angle4 = (angle + 180) % 360;


                    if (speed4 > 0) {

                        speed4 = (speed -1);

                    }

                }
                if (ballRect4.right >= ballRect3.left &&
                    ballRect4.left <= ballRect3.right &&
                    ballRect4.bottom >= ballRect3.top &&
                    ballRect4.top <= ballRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ



                    angle4 = (angle + 180) % 360;


                    if (speed4 > 0) {

                        speed4 = (speed -1);

                    }

                }
                if (ballRect4.right >= wallRect1.left &&
                    ballRect4.left <= wallRect1.right &&
                    ballRect4.bottom >= wallRect1.top &&
                    ballRect4.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle4 = 200 - angle4;
                    if (speed4 > 0) {

                        speed4 -= 1;

                    }

                }

                if (ballRect4.right >= wallRect2.left &&
                    ballRect4.left <= wallRect2.right &&
                    ballRect4.bottom >= wallRect2.top &&
                    ballRect4.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle4 = 250 + angle4;

                    if (speed4 > 0) {

                        speed4 -= 1;

                    }
                }

                if (ballRect4.right >= wallRect3.left &&
                    ballRect4.left <= wallRect3.right &&
                    ballRect4.bottom >= wallRect3.top &&
                    ballRect4.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle4 = 250 + angle4;
                    if (speed4 > 0) {

                        speed4 -= 1;

                    }

                }

                if (ballRect4.right >= wallRect4.left &&
                    ballRect4.left <= wallRect4.right &&
                    ballRect4.bottom >= wallRect4.top &&
                    ballRect4.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle4 = 250 + angle4;
                    if (speed4 > 0) {

                        speed4 -= 1;

                    }

                }

                if (ballRect4.right >= wallRect5.left &&
                    ballRect4.left <= wallRect5.right &&
                    ballRect4.bottom >= wallRect5.top &&
                    ballRect4.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle4 = 250 + angle4;
                    if (speed4 > 0) {

                        speed4 -= 1;

                    }
                }

                if (ballRect4.right >= wallRect6.left &&
                    ballRect4.left <= wallRect6.right &&
                    ballRect4.bottom >= wallRect6.top &&
                    ballRect4.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle4 = 200 - angle4;
                    if (speed4 > 0) {

                        speed4 -= 1;

                    }

                }
                ///////////////////////////////////////////////////////////////////////////////b5
                if (ballRect5.right >= ballRect.left &&
                    ballRect5.left <= ballRect.right &&
                    ballRect5.bottom >= ballRect.top &&
                    ballRect5.top <= ballRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle5 = (angle + 180) % 360;



                    if (speed5 > 0) {

                        speed5 = (speed -1);

                    }

                }
                if (ballRect5.right >= ballRect1.left &&
                    ballRect5.left <= ballRect1.right &&
                    ballRect5.bottom >= ballRect1.top &&
                    ballRect5.top <= ballRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ


                    angle5 = (angle + 180) % 360;


                    if (speed5 > 0) {

                        speed5 = (speed- 1);


                    }

                }
                if (ballRect5.right >= ballRect2.left &&
                    ballRect5.left <= ballRect2.right &&
                    ballRect5.bottom >= ballRect2.top &&
                    ballRect5.top <= ballRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ


                    angle5 = (angle + 180) % 360;


                    if (speed5 > 0) {

                        speed5 = (speed - 1);


                    }

                }
                if (ballRect5.right >= ballRect3.left &&
                    ballRect5.left <= ballRect3.right &&
                    ballRect5.bottom >= ballRect3.top &&
                    ballRect5.top <= ballRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ



                    angle5 = (angle + 180) % 360;


                    if (speed5 > 0) {

                        speed5 = (speed - 1);


                    }
                }
                if (ballRect5.right >= ballRect4.left &&
                    ballRect5.left <= ballRect4.right &&
                    ballRect5.bottom >= ballRect4.top &&
                    ballRect5.top <= ballRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle5 = (angle+ 180) % 360;



                    if (speed5 > 0) {
                        speed5 = (speed - 1);


                    }
                }
                if (ballRect5.right >= wallRect1.left &&
                    ballRect5.left <= wallRect1.right &&
                    ballRect5.bottom >= wallRect1.top &&
                    ballRect5.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle5 = 200 - angle5;
                    if (speed5 > 0) {

                        speed5 -= 1;

                    }

                }

                if (ballRect5.right >= wallRect2.left &&
                    ballRect5.left <= wallRect2.right &&
                    ballRect5.bottom >= wallRect2.top &&
                    ballRect5.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle5 = 250 + angle5;
                    if (speed5 > 0) {

                        speed5 -= 1;

                    }

                }

                if (ballRect5.right >= wallRect3.left &&
                    ballRect5.left <= wallRect3.right &&
                    ballRect5.bottom >= wallRect3.top &&
                    ballRect5.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle5 = 250 + angle5;
                    if (speed5 > 0) {

                        speed5 -= 1;

                    }

                }

                if (ballRect5.right >= wallRect4.left &&
                    ballRect5.left <= wallRect4.right &&
                    ballRect5.bottom >= wallRect4.top &&
                    ballRect5.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle5 = 250 + angle5;
                    if (speed5 > 0) {

                        speed5 -= 1;

                    }

                }

                if (ballRect5.right >= wallRect5.left &&
                    ballRect5.left <= wallRect5.right &&
                    ballRect5.bottom >= wallRect5.top &&
                    ballRect5.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle5 = 250 + angle5;
                    if (speed5 > 0) {

                        speed5 -= 1;

                    }

                }

                if (ballRect5.right >= wallRect6.left &&
                    ballRect5.left <= wallRect6.right &&
                    ballRect5.bottom >= wallRect6.top &&
                    ballRect5.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle5 = 200 - angle5;
                    if (speed5 > 0) {

                        speed5 -= 1;

                    }

                }
                ///////////////////////////////////////////////////////////////////////////////b6
                if (ballRect6.right >= ballRect.left &&
                    ballRect6.left <= ballRect.right &&
                    ballRect6.bottom >= ballRect.top &&
                    ballRect6.top <= ballRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle6 = (angle + 180) % 360;



                    if (speed6 > 0) {

                        speed6 = (speed -1);

                    }
                }
                if (ballRect6.right >= ballRect1.left &&
                    ballRect6.left <= ballRect1.right &&
                    ballRect6.bottom >= ballRect1.top &&
                    ballRect6.top <= ballRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle6 = (angle + 180) % 360;



                    if (speed6 > 0) {

                        speed6 = (speed - 1);


                    }

                }
                if (ballRect6.right >= ballRect2.left &&
                    ballRect6.left <= ballRect2.right &&
                    ballRect6.bottom >= ballRect2.top &&
                    ballRect6.top <= ballRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle6 = (angle + 180) % 360;



                    if (speed6 > 0) {

                        speed6 = (speed - 1);


                    }

                }
                if (ballRect6.right >= ballRect3.left &&
                    ballRect6.left <= ballRect3.right &&
                    ballRect6.bottom >= ballRect3.top &&
                    ballRect6.top <= ballRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ


                    angle6 = (angle + 180) % 360;



                    if (speed6 > 0) {

                        speed6 = (speed- 1);


                    }

                }
                if (ballRect6.right >= ballRect4.left &&
                    ballRect6.left <= ballRect4.right &&
                    ballRect6.bottom >= ballRect4.top &&
                    ballRect6.top <= ballRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle6 = (angle + 180) % 360;



                    if (speed6 > 0) {

                        speed6 = (speed - 1);


                    }

                }
                if (ballRect6.right >= ballRect5.left &&
                    ballRect6.left <= ballRect5.right &&
                    ballRect6.bottom >= ballRect5.top &&
                    ballRect6.top <= ballRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle6 = (angle + 180) % 360;



                    if (speed6 > 0) {

                        speed6 = (speed - 1);


                    }

                }
                if (ballRect6.right >= wallRect1.left &&
                    ballRect6.left <= wallRect1.right &&
                    ballRect6.bottom >= wallRect1.top &&
                    ballRect6.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle6 = 200 - angle6;
                    if (speed6 > 0) {

                        speed6 -= 1;

                    }

                }

                if (ballRect6.right >= wallRect2.left &&
                    ballRect6.left <= wallRect2.right &&
                    ballRect6.bottom >= wallRect2.top &&
                    ballRect6.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle6 = 250 + angle6;
                    if (speed6 > 0) {

                        speed6 -= 1;

                    }

                }

                if (ballRect6.right >= wallRect3.left &&
                    ballRect6.left <= wallRect3.right &&
                    ballRect6.bottom >= wallRect3.top &&
                    ballRect6.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle6 = 250 + angle6;
                    if (speed6 > 0) {

                        speed6 -= 1;

                    }

                }

                if (ballRect6.right >= wallRect4.left &&
                    ballRect6.left <= wallRect4.right &&
                    ballRect6.bottom >= wallRect4.top &&
                    ballRect6.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle6 = 250 + angle6;
                    if (speed6 > 0) {

                        speed6 -= 1;

                    }

                }

                if (ballRect6.right >= wallRect5.left &&
                    ballRect6.left <= wallRect5.right &&
                    ballRect6.bottom >= wallRect5.top &&
                    ballRect6.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle6 = 250 + angle6;
                    if (speed6 > 0) {

                        speed6 -= 1;

                    }

                }

                if (ballRect6.right >= wallRect6.left &&
                    ballRect6.left <= wallRect6.right &&
                    ballRect6.bottom >= wallRect6.top &&
                    ballRect6.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle6 = 200 - angle6;
                    if (speed6 > 0) {

                        speed6 -= 1;

                    }

                }
                ///////////////////////////////////////////////////////////////////////////////b7
                if (ballRect7.right >= ballRect.left &&
                    ballRect7.left <= ballRect.right &&
                    ballRect7.bottom >= ballRect.top &&
                    ballRect7.top <= ballRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle7 = (angle + 180) % 360;



                    if (speed7 > 0) {

                        speed7 = (speed -1);

                    }

                }
                if (ballRect7.right >= ballRect1.left &&
                    ballRect7.left <= ballRect1.right &&
                    ballRect7.bottom >= ballRect1.top &&
                    ballRect7.top <= ballRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle7 = (angle + 180) % 360;



                    if (speed7 > 0) {

                        speed7 = (speed - 1);

                    }



                }
                if (ballRect7.right >= ballRect2.left &&
                    ballRect7.left <= ballRect2.right &&
                    ballRect7.bottom >= ballRect2.top &&
                    ballRect7.top <= ballRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle7 = (angle + 180) % 360;



                    if (speed7 > 0) {

                        speed7 = (speed- 1);

                    }

                    }


                if (ballRect7.right >= ballRect3.left &&
                    ballRect7.left <= ballRect3.right &&
                    ballRect7.bottom >= ballRect3.top &&
                    ballRect7.top <= ballRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle7 = (angle + 180) % 360;



                    if (speed7 > 0) {

                        speed7 = (speed - 1);

                    }



                }
                if (ballRect7.right >= ballRect4.left &&
                    ballRect7.left <= ballRect4.right &&
                    ballRect7.bottom >= ballRect4.top &&
                    ballRect7.top <= ballRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle7 = (angle + 180) % 360;



                    if (speed7 > 0) {

                        speed7 = (speed - 1);

                    }


                }
                if (ballRect7.right >= ballRect5.left &&
                    ballRect7.left <= ballRect5.right &&
                    ballRect7.bottom >= ballRect5.top &&
                    ballRect7.top <= ballRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle7 = (angle + 180) % 360;



                    if (speed7 > 0) {

                        speed7 = (speed - 1);

                    }


                }
                if (ballRect7.right >= ballRect6.left &&
                    ballRect7.left <= ballRect6.right &&
                    ballRect7.bottom >= ballRect6.top &&
                    ballRect7.top <= ballRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle7 = (angle + 180) % 360;



                    if (speed7 > 0) {

                        speed7 = (speed - 1);

                    }


                }
                if (ballRect7.right >= wallRect1.left &&
                    ballRect7.left <= wallRect1.right &&
                    ballRect7.bottom >= wallRect1.top &&
                    ballRect7.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle7 = 200 - angle7;
                    if (speed7 > 0) {

                        speed7 -= 1;

                    }

                }

                if (ballRect7.right >= wallRect2.left &&
                    ballRect7.left <= wallRect2.right &&
                    ballRect7.bottom >= wallRect2.top &&
                    ballRect7.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle7 = 250 + angle7;
                    if (speed7 > 0) {

                        speed7 -= 1;

                    }

                }

                if (ballRect7.right >= wallRect3.left &&
                    ballRect7.left <= wallRect3.right &&
                    ballRect7.bottom >= wallRect3.top &&
                    ballRect7.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle7 = 250 + angle7;
                    if (speed7 > 0) {

                        speed7 -= 1;

                    }

                }

                if (ballRect7.right >= wallRect4.left &&
                    ballRect7.left <= wallRect4.right &&
                    ballRect7.bottom >= wallRect4.top &&
                    ballRect7.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle7 = 250 + angle7;
                    if (speed7 > 0) {

                        speed7 -= 1;

                    }

                }

                if (ballRect7.right >= wallRect5.left &&
                    ballRect7.left <= wallRect5.right &&
                    ballRect7.bottom >= wallRect5.top &&
                    ballRect7.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle7 = 250 + angle7;
                    if (speed7 > 0) {

                        speed7 -= 1;

                    }

                }

                if (ballRect7.right >= wallRect6.left &&
                    ballRect7.left <= wallRect6.right &&
                    ballRect7.bottom >= wallRect6.top &&
                    ballRect7.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle7 = 200 - angle7;
                    if (speed7 > 0) {

                        speed7 -= 1;

                    }

                }
                /////////////////////////////////////////////////////////////////////////////beight
                if (eightRect.right >= ballRect.left &&
                    eightRect.left <= ballRect.right &&
                    eightRect.bottom >= ballRect.top &&
                    eightRect.top <= ballRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle8 = (angle + 180) % 360;



                    if (speed8 > 0) {

                        speed8 = (speed- 1);

                    }

                }
                if (eightRect.right >= ballRect1.left &&
                    eightRect.left <= ballRect1.right &&
                    eightRect.bottom >= ballRect1.top &&
                    eightRect.top <= ballRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle8 = (angle + 180) % 360;



                    if (speed8 > 0) {

                        speed8 = (speed - 1);

                    }

                }
                if (eightRect.right >= ballRect2.left &&
                    eightRect.left <= ballRect2.right &&
                    eightRect.bottom >= ballRect2.top &&
                    eightRect.top <= ballRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle8 = (angle + 180) % 360;



                    if (speed8 > 0) {

                        speed8 = (speed- 1);

                    }

                }
                if (eightRect.right >= ballRect3.left &&
                    eightRect.left <= ballRect3.right &&
                    eightRect.bottom >= ballRect3.top &&
                    eightRect.top <= ballRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle8 = (angle + 180) % 360;



                    if (speed8 > 0) {

                        speed8 = (speed - 1);

                    }

                }
                if (eightRect.right >= ballRect4.left &&
                    eightRect.left <= ballRect4.right &&
                    eightRect.bottom >= ballRect4.top &&
                    eightRect.top <= ballRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle8 = (angle + 180) % 360;



                    if (speed8 > 0) {

                        speed8 = (speed - 1);

                    }

                }
                if (eightRect.right >= ballRect5.left &&
                    eightRect.left <= ballRect5.right &&
                    eightRect.bottom >= ballRect5.top &&
                    eightRect.top <= ballRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle8 = (angle + 180) % 360;



                    if (speed8 > 0) {

                        speed8 = (speed- 1);

                    }

                }
                if (eightRect.right >= ballRect6.left &&
                    eightRect.left <= ballRect6.right &&
                    eightRect.bottom >= ballRect6.top &&
                    eightRect.top <= ballRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle8 = (angle + 180) % 360;



                    if (speed8 > 0) {

                        speed8 = (speed- 1);

                    }

                }
                if (eightRect.right >= ballRect7.left &&
                    eightRect.left <= ballRect7.right &&
                    eightRect.bottom >= ballRect7.top &&
                    eightRect.top <= ballRect7.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle8 = (angle + 180) % 360;



                    if (speed8 > 0) {

                        speed8 = (speed - 1);

                    }

                }

                if (eightRect.right >= wallRect1.left &&
                    eightRect.left <= wallRect1.right &&
                    eightRect.bottom >= wallRect1.top &&
                    eightRect.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle8 = 200 - angle8;
                    if (speed8 > 0) {

                        speed8 -= 1;

                    }

                }

                if (eightRect.right >= wallRect2.left &&
                    eightRect.left <= wallRect2.right &&
                    eightRect.bottom >= wallRect2.top &&
                    eightRect.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle8 = 250 + angle8;
                    if (speed8 > 0) {

                        speed8 -= 1;

                    }

                }

                if (eightRect.right >= wallRect3.left &&
                    eightRect.left <= wallRect3.right &&
                    eightRect.bottom >= wallRect3.top &&
                    eightRect.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle8 = 250 + angle8;
                    if (speed8 > 0) {

                        speed8 -= 1;

                    }

                }

                if (eightRect.right >= wallRect4.left &&
                    eightRect.left <= wallRect4.right &&
                    eightRect.bottom >= wallRect4.top &&
                    eightRect.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle8 = 250 + angle8;
                    if (speed8 > 0) {

                        speed8 -= 1;

                    }

                }

                if (eightRect.right >= wallRect5.left &&
                    eightRect.left <= wallRect5.right &&
                    eightRect.bottom >= wallRect5.top &&
                    eightRect.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle8 = 250 + angle8;
                    if (speed8 > 0) {

                        speed8 -= 1;

                    }

                }

                if (eightRect.right >= wallRect6.left &&
                    eightRect.left <= wallRect6.right &&
                    eightRect.bottom >= wallRect6.top &&
                    eightRect.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle8 = 200 - angle8;
                    if (speed8 > 0) {

                        speed8 -= 1;

                    }

                }
                ///////////////////////////////////////////////////////////////////////////////b9
                if (ballRect9.right >= ballRect.left &&
                    ballRect9.left <= ballRect.right &&
                    ballRect9.bottom >= ballRect.top &&
                    ballRect9.top <= ballRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle9 = (angle + 180) % 360;



                    if (speed9 > 0) {

                        speed9 = (speed- 1);

                    }


                }
                if (ballRect9.right >= ballRect1.left &&
                    ballRect9.left <= ballRect1.right &&
                    ballRect9.bottom >= ballRect1.top &&
                    ballRect9.top <= ballRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle9 = (angle + 180) % 360;



                    if (speed9 > 0) {

                        speed9 = (speed - 1);

                    }


                }
                if (ballRect9.right >= ballRect2.left &&
                    ballRect9.left <= ballRect2.right &&
                    ballRect9.bottom >= ballRect2.top &&
                    ballRect9.top <= ballRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle9 = (angle + 180) % 360;



                    if (speed9 > 0) {

                        speed9 = (speed - 1);

                    }

                }
                if (ballRect9.right >= ballRect3.left &&
                    ballRect9.left <= ballRect3.right &&
                    ballRect9.bottom >= ballRect3.top &&
                    ballRect9.top <= ballRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle9 = (angle + 180) % 360;



                    if (speed9 > 0) {

                        speed9 = (speed- 1);

                    }

                }
                if (ballRect9.right >= ballRect4.left &&
                    ballRect9.left <= ballRect4.right &&
                    ballRect9.bottom >= ballRect4.top &&
                    ballRect9.top <= ballRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle9 = (angle + 180) % 360;



                    if (speed9 > 0) {

                        speed9 = (speed- 1);

                    }

                }
                if (ballRect9.right >= ballRect5.left &&
                    ballRect9.left <= ballRect5.right &&
                    ballRect9.bottom >= ballRect5.top &&
                    ballRect9.top <= ballRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle9 = (angle + 180) % 360;



                    if (speed9 > 0) {

                        speed9 = (speed - 1);

                    }
                }
                if (ballRect9.right >= ballRect6.left &&
                    ballRect9.left <= ballRect6.right &&
                    ballRect9.bottom >= ballRect6.top &&
                    ballRect9.top <= ballRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle9 = (angle + 180) % 360;



                    if (speed9 > 0) {

                        speed9 = (speed - 1);

                    }

                }
                if (ballRect9.right >= ballRect7.left &&
                    ballRect9.left <= ballRect7.right &&
                    ballRect9.bottom >= ballRect7.top &&
                    ballRect9.top <= ballRect7.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle9 = (angle + 180) % 360;



                    if (speed9 > 0) {

                        speed9 = (speed - 1);

                    }

                }
                if (ballRect9.right >= eightRect.left &&
                    ballRect9.left <= eightRect.right &&
                    ballRect9.bottom >= eightRect.top &&
                    ballRect9.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle9 = (angle + 180) % 360;



                    if (speed9 > 0) {

                        speed9 = (speed - 1);

                    }

                }
                if (ballRect9.right >= wallRect1.left &&
                    ballRect9.left <= wallRect1.right &&
                    ballRect9.bottom >= wallRect1.top &&
                    ballRect9.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle9 = 200 - angle9;
                         if (speed9 > 0) {

                        speed9 -= 1;

                    }

                }

                if (ballRect9.right >= wallRect2.left &&
                    ballRect9.left <= wallRect2.right &&
                    ballRect9.bottom >= wallRect2.top &&
                    ballRect9.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle9 = 250 + angle9;
                    if (speed9 > 0) {

                        speed9 -= 1;

                    }

                }

                if (ballRect9.right >= wallRect3.left &&
                    ballRect9.left <= wallRect3.right &&
                    ballRect9.bottom >= wallRect3.top &&
                    ballRect9.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle9 = 250 + angle9;
                    if (speed9 > 0) {

                        speed9 -= 1;

                    }

                }

                if (ballRect9.right >= wallRect4.left &&
                    ballRect9.left <= wallRect4.right &&
                    ballRect9.bottom >= wallRect4.top &&
                    ballRect9.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle9 = 250 + angle9;
                    if (speed9 > 0) {

                        speed9 -= 1;

                    }

                }

                if (ballRect9.right >= wallRect5.left &&
                    ballRect9.left <= wallRect5.right &&
                    ballRect9.bottom >= wallRect5.top &&
                    ballRect9.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle9 = 250 + angle9;
                    if (speed9 > 0) {

                        speed9 -= 1;

                    }

                }

                if (ballRect9.right >= wallRect6.left &&
                    ballRect9.left <= wallRect6.right &&
                    ballRect9.bottom >= wallRect6.top &&
                    ballRect9.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle9 = 200 - angle9;
                    if (speed9 > 0) {

                        speed9 -= 1;

                    }

                }
                ///////////////////////////////////////////////////////////////////////////////b10
                if (ballRect10.right >= ballRect.left &&
                    ballRect10.left <= ballRect.right &&
                    ballRect10.bottom >= ballRect.top &&
                    ballRect10.top <= ballRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle10 = (angle + 180) % 360;



                    if (speed10 > 0) {

                        speed10 = (speed - 1);

                    }

                }
                if (ballRect10.right >= ballRect1.left &&
                    ballRect10.left <= ballRect1.right &&
                    ballRect10.bottom >= ballRect1.top &&
                    ballRect10.top <= ballRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle10 = (angle + 180) % 360;



                    if (speed10 > 0) {

                        speed10 = (speed - 1);

                    }

                }
                if (ballRect10.right >= ballRect2.left &&
                    ballRect10.left <= ballRect2.right &&
                    ballRect10.bottom >= ballRect2.top &&
                    ballRect10.top <= ballRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle10 = (angle + 180) % 360;



                    if (speed10 > 0) {

                        speed10 = (speed - 1);

                    }

                }
                if (ballRect10.right >= ballRect3.left &&
                    ballRect10.left <= ballRect3.right &&
                    ballRect10.bottom >= ballRect3.top &&
                    ballRect10.top <= ballRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle10 = (angle + 180) % 360;



                    if (speed10 > 0) {

                        speed10 = (speed - 1);

                    }

                }
                if (ballRect10.right >= ballRect4.left &&
                    ballRect10.left <= ballRect4.right &&
                    ballRect10.bottom >= ballRect4.top &&
                    ballRect10.top <= ballRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle10 = (angle + 180) % 360;



                    if (speed10 > 0) {

                        speed10 = (speed - 1);

                    }

                }
                if (ballRect10.right >= ballRect5.left &&
                    ballRect10.left <= ballRect5.right &&
                    ballRect10.bottom >= ballRect5.top &&
                    ballRect10.top <= ballRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle10 = (angle + 180) % 360;



                    if (speed10 > 0) {

                        speed10 = (speed - 1);

                    }

                }
                if (ballRect10.right >= ballRect6.left &&
                    ballRect10.left <= ballRect6.right &&
                    ballRect10.bottom >= ballRect6.top &&
                    ballRect10.top <= ballRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle10 = (angle + 180) % 360;



                    if (speed10 > 0) {

                        speed10 = (speed - 1);

                    }

                }
                if (ballRect10.right >= ballRect7.left &&
                    ballRect10.left <= ballRect7.right &&
                    ballRect10.bottom >= ballRect7.top &&
                    ballRect10.top <= ballRect7.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle10 = (angle + 180) % 360;



                    if (speed10 > 0) {

                        speed10 = (speed - 1);

                    }

                }
                if (ballRect10.right >= eightRect.left &&
                    ballRect10.left <= eightRect.right &&
                    ballRect10.bottom >= eightRect.top &&
                    ballRect10.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle10 = (angle + 180) % 360;



                    if (speed10 > 0) {

                        speed10 = (speed - 1);

                    }

                }
                if (ballRect10.right >= ballRect9.left &&
                    ballRect10.left <= ballRect9.right &&
                    ballRect10.bottom >= ballRect9.top &&
                    ballRect10.top <= ballRect9.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle10 = (angle + 180) % 360;



                    if (speed10 > 0) {

                        speed10 = (speed - 1);

                    }

                }
                if (ballRect10.right >= wallRect1.left &&
                    ballRect10.left <= wallRect1.right &&
                    ballRect10.bottom >= wallRect1.top &&
                    ballRect10.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle10 = 200 - angle10;
                    if (speed10 > 0) {

                        speed10 -= 1;

                    }

                }

                if (ballRect10.right >= wallRect2.left &&
                    ballRect10.left <= wallRect2.right &&
                    ballRect10.bottom >= wallRect2.top &&
                    ballRect10.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle10 = 250 + angle10;
                    if (speed10 > 0) {

                        speed10 -= 1;

                    }


                }

                if (ballRect10.right >= wallRect3.left &&
                    ballRect10.left <= wallRect3.right &&
                    ballRect10.bottom >= wallRect3.top &&
                    ballRect10.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle10 = 250 + angle10;
                    if (speed10 > 0) {

                        speed10 -= 1;

                    }


                }

                if (ballRect10.right >= wallRect4.left &&
                    ballRect10.left <= wallRect4.right &&
                    ballRect10.bottom >= wallRect4.top &&
                    ballRect10.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle10 = 250 + angle10;
                    if (speed10 > 0) {

                        speed10 -= 1;

                    }


                }

                if (ballRect10.right >= wallRect5.left &&
                    ballRect10.left <= wallRect5.right &&
                    ballRect10.bottom >= wallRect5.top &&
                    ballRect10.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle10 = 250 + angle10;
                    if (speed10 > 0) {

                        speed10 -= 1;

                    }


                }

                if (ballRect10.right >= wallRect6.left &&
                    ballRect10.left <= wallRect6.right &&
                    ballRect10.bottom >= wallRect6.top &&
                    ballRect10.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle10 = 200 - angle10;
                    if (speed10 > 0) {

                        speed10 -= 1;

                    }


                }
                ///////////////////////////////////////////////////////////////////////////////b11
                if (ballRect11.right >= ballRect.left &&
                    ballRect11.left <= ballRect.right &&
                    ballRect11.bottom >= ballRect.top &&
                    ballRect11.top <= ballRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle11 = (angle + 180) % 360;



                    if (speed11> 0) {

                        speed11 = (speed - 1);

                    }

                }
                if (ballRect11.right >= ballRect1.left &&
                    ballRect11.left <= ballRect1.right &&
                    ballRect11.bottom >= ballRect1.top &&
                    ballRect11.top <= ballRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle11 = (angle + 180) % 360;



                    if (speed11 > 0) {

                        speed11 = (speed - 1);

                    }

                }
                if (ballRect11.right >= ballRect2.left &&
                    ballRect11.left <= ballRect2.right &&
                    ballRect11.bottom >= ballRect2.top &&
                    ballRect11.top <= ballRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle11 = (angle + 180) % 360;



                    if (speed11 > 0) {

                        speed11 = (speed - 1);

                    }

                }
                if (ballRect11.right >= ballRect3.left &&
                    ballRect11.left <= ballRect3.right &&
                    ballRect11.bottom >= ballRect3.top &&
                    ballRect11.top <= ballRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle11 = (angle + 180) % 360;



                    if (speed11 > 0) {

                        speed11 = (speed - 1);

                    }
                }
                if (ballRect11.right >= ballRect4.left &&
                    ballRect11.left <= ballRect4.right &&
                    ballRect11.bottom >= ballRect4.top &&
                    ballRect11.top <= ballRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle11 = (angle + 180) % 360;



                    if (speed11 > 0) {

                        speed11 = (speed - 1);

                    }
                }
                if (ballRect11.right >= ballRect5.left &&
                    ballRect11.left <= ballRect5.right &&
                    ballRect11.bottom >= ballRect5.top &&
                    ballRect11.top <= ballRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle11 = (angle + 180) % 360;



                    if (speed11 > 0) {

                        speed11 = (speed - 1);

                    }
                }
                if (ballRect11.right >= ballRect6.left &&
                    ballRect11.left <= ballRect6.right &&
                    ballRect11.bottom >= ballRect6.top &&
                    ballRect11.top <= ballRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle11 = (angle + 180) % 360;



                    if (speed11 > 0) {

                        speed11 = (speed - 1);

                    }

                }
                if (ballRect11.right >= ballRect7.left &&
                    ballRect11.left <= ballRect7.right &&
                    ballRect11.bottom >= ballRect7.top &&
                    ballRect11.top <= ballRect7.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle11 = (angle + 180) % 360;



                    if (speed11 > 0) {

                        speed11 = (speed - 1);

                    }
                }
                if (ballRect11.right >= eightRect.left &&
                    ballRect11.left <= eightRect.right &&
                    ballRect11.bottom >= eightRect.top &&
                    ballRect11.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle11 = (angle + 180) % 360;



                    if (speed11 > 0) {

                        speed11 = (speed - 1);

                    }

                }
                if (ballRect11.right >= ballRect9.left &&
                    ballRect11.left <= ballRect9.right &&
                    ballRect11.bottom >= ballRect9.top &&
                    ballRect11.top <= ballRect9.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle11 = (angle + 180) % 360;



                    if (speed11 > 0) {

                        speed11 = (speed - 1);

                    }

                }
                if (ballRect11.right >= ballRect10.left &&
                    ballRect11.left <= ballRect10.right &&
                    ballRect11.bottom >= ballRect10.top &&
                    ballRect11.top <= ballRect10.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle11 = (angle + 180) % 360;



                    if (speed11 > 0) {

                        speed11 = (speed - 1);

                    }

                }
                if (ballRect11.right >= wallRect1.left &&
                    ballRect11.left <= wallRect1.right &&
                    ballRect11.bottom >= wallRect1.top &&
                    ballRect11.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle11 = 200 - angle11;

                    if (speed11 > 0) {

                        speed11 -= 1;

                    }
                }

                if (ballRect11.right >= wallRect2.left &&
                    ballRect11.left <= wallRect2.right &&
                    ballRect11.bottom >= wallRect2.top &&
                    ballRect11.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle11 = 250 + angle11;
                    if (speed11 > 0) {

                        speed11 -= 1;

                    }

                }

                if (ballRect11.right >= wallRect3.left &&
                    ballRect11.left <= wallRect3.right &&
                    ballRect11.bottom >= wallRect3.top &&
                    ballRect11.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle11 = 250 + angle11;

                    if (speed11 > 0) {

                        speed11 -= 1;

                    }
                }

                if (ballRect11.right >= wallRect4.left &&
                    ballRect11.left <= wallRect4.right &&
                    ballRect11.bottom >= wallRect4.top &&
                    ballRect11.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle11 = 250 + angle11;
                    if (speed11 > 0) {

                        speed11 -= 1;

                    }

                }

                if (ballRect11.right >= wallRect5.left &&
                    ballRect11.left <= wallRect5.right &&
                    ballRect11.bottom >= wallRect5.top &&
                    ballRect11.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle11 = 250 + angle11;
                    if (speed11 > 0) {

                        speed11 -= 1;

                    }

                }

                if (ballRect11.right >= wallRect6.left &&
                    ballRect11.left <= wallRect6.right &&
                    ballRect11.bottom >= wallRect6.top &&
                    ballRect11.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle11 = 200 - angle11;
                    if (speed11 > 0) {

                        speed11 -= 1;

                    }


                }
                ///////////////////////////////////////////////////////////////////////////////b12
                if (ballRect12.right >= ballRect.left &&
                    ballRect12.left <= ballRect.right &&
                    ballRect12.bottom >= ballRect.top &&
                    ballRect12.top <= ballRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle12 = (angle + 180) % 360;



                    if (speed12 > 0) {

                        speed12 = (speed - 1);

                    }

                }
                if (ballRect12.right >= ballRect1.left &&
                    ballRect12.left <= ballRect1.right &&
                    ballRect12.bottom >= ballRect1.top &&
                    ballRect12.top <= ballRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle12 = (angle + 180) % 360;



                    if (speed12 > 0) {

                        speed12 = (speed - 1);

                    }

                }
                if (ballRect12.right >= ballRect2.left &&
                    ballRect12.left <= ballRect2.right &&
                    ballRect12.bottom >= ballRect2.top &&
                    ballRect12.top <= ballRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle12 = (angle + 180) % 360;



                    if (speed12 > 0) {

                        speed12 = (speed - 1);

                    }

                }
                if (ballRect12.right >= ballRect3.left &&
                    ballRect12.left <= ballRect3.right &&
                    ballRect12.bottom >= ballRect3.top &&
                    ballRect12.top <= ballRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle12 = (angle + 180) % 360;



                    if (speed12 > 0) {

                        speed12 = (speed - 1);

                    }

                }
                if (ballRect12.right >= ballRect4.left &&
                    ballRect12.left <= ballRect4.right &&
                    ballRect12.bottom >= ballRect4.top &&
                    ballRect12.top <= ballRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle12 = (angle + 180) % 360;



                    if (speed12 > 0) {

                        speed12 = (speed - 1);

                    }

                }
                if (ballRect12.right >= ballRect5.left &&
                    ballRect12.left <= ballRect5.right &&
                    ballRect12.bottom >= ballRect5.top &&
                    ballRect12.top <= ballRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle12 = (angle + 180) % 360;



                    if (speed12 > 0) {

                        speed12 = (speed - 1);

                    }

                }
                if (ballRect12.right >= ballRect6.left &&
                    ballRect12.left <= ballRect6.right &&
                    ballRect12.bottom >= ballRect6.top &&
                    ballRect12.top <= ballRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle12 = (angle + 180) % 360;



                    if (speed12 > 0) {

                        speed12 = (speed - 1);

                    }

                }
                if (ballRect12.right >= ballRect7.left &&
                    ballRect12.left <= ballRect7.right &&
                    ballRect12.bottom >= ballRect7.top &&
                    ballRect12.top <= ballRect7.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle12 = (angle + 180) % 360;



                    if (speed12 > 0) {

                        speed12 = (speed - 1);

                    }

                }
                if (ballRect12.right >= eightRect.left &&
                    ballRect12.left <= eightRect.right &&
                    ballRect12.bottom >= eightRect.top &&
                    ballRect12.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle12 = (angle + 180) % 360;



                    if (speed12 > 0) {

                        speed12 = (speed - 1);

                    }

                }
                if (ballRect12.right >= ballRect9.left &&
                    ballRect12.left <= ballRect9.right &&
                    ballRect12.bottom >= ballRect9.top &&
                    ballRect12.top <= ballRect9.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle12 = (angle + 180) % 360;



                    if (speed12 > 0) {

                        speed12 = (speed - 1);

                    }

                }
                if (ballRect12.right >= ballRect10.left &&
                    ballRect12.left <= ballRect10.right &&
                    ballRect12.bottom >= ballRect10.top &&
                    ballRect12.top <= ballRect10.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle12 = (angle + 180) % 360;



                    if (speed12 > 0) {

                        speed12 = (speed - 1);

                    }

                }
                if (ballRect12.right >= ballRect11.left &&
                    ballRect12.left <= ballRect11.right &&
                    ballRect12.bottom >= ballRect11.top &&
                    ballRect12.top <= ballRect11.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle12 = (angle + 180) % 360;



                    if (speed12 > 0) {

                        speed12 = (speed - 1);

                    }

                }
                if (ballRect12.right >= wallRect1.left &&
                    ballRect12.left <= wallRect1.right &&
                    ballRect12.bottom >= wallRect1.top &&
                    ballRect12.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle12 = 200 - angle12;
                    if (speed12 > 0) {

                        speed12 -= 1;

                    }

                }

                if (ballRect12.right >= wallRect2.left &&
                    ballRect12.left <= wallRect2.right &&
                    ballRect12.bottom >= wallRect2.top &&
                    ballRect12.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle12 = 250 + angle12;
                    if (speed12 > 0) {

                        speed12 -= 1;

                    }

                }

                if (ballRect12.right >= wallRect3.left &&
                    ballRect12.left <= wallRect3.right &&
                    ballRect12.bottom >= wallRect3.top &&
                    ballRect12.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle12 = 250 + angle12;
                    if (speed12 > 0) {

                        speed12 -= 1;

                    }

                }

                if (ballRect12.right >= wallRect4.left &&
                    ballRect12.left <= wallRect4.right &&
                    ballRect12.bottom >= wallRect4.top &&
                    ballRect12.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle12 = 250 + angle12;

                    if (speed12 > 0) {

                        speed12 -= 1;

                    }
                }

                if (ballRect12.right >= wallRect5.left &&
                    ballRect12.left <= wallRect5.right &&
                    ballRect12.bottom >= wallRect5.top &&
                    ballRect12.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle12 = 250 + angle12;
                    if (speed12 > 0) {

                        speed12 -= 1;

                    }

                }

                if (ballRect12.right >= wallRect6.left &&
                    ballRect12.left <= wallRect6.right &&
                    ballRect12.bottom >= wallRect6.top &&
                    ballRect12.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle12 = 200 - angle12;
                    if (speed12 > 0) {

                        speed12 -= 1;

                    }

                }
                ///////////////////////////////////////////////////////////////////////////////b13
                if (ballRect13.right >= ballRect.left &&
                    ballRect13.left <= ballRect.right &&
                    ballRect13.bottom >= ballRect.top &&
                    ballRect13.top <= ballRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle13 = (angle + 180) % 360;



                    if (speed13 > 0) {

                        speed13 = (speed - 1);

                    }
                }
                if (ballRect13.right >= ballRect1.left &&
                    ballRect13.left <= ballRect1.right &&
                    ballRect13.bottom >= ballRect1.top &&
                    ballRect13.top <= ballRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle13 = (angle + 180) % 360;



                    if (speed13 > 0) {

                        speed13 = (speed - 1);

                    }
                }
                if (ballRect13.right >= ballRect2.left &&
                    ballRect13.left <= ballRect2.right &&
                    ballRect13.bottom >= ballRect2.top &&
                    ballRect13.top <= ballRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle13 = (angle + 180) % 360;



                    if (speed13 > 0) {

                        speed13 = (speed - 1);

                    }

                }
                if (ballRect13.right >= ballRect3.left &&
                    ballRect13.left <= ballRect3.right &&
                    ballRect13.bottom >= ballRect3.top &&
                    ballRect13.top <= ballRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle13 = (angle + 180) % 360;



                    if (speed13 > 0) {

                        speed13 = (speed - 1);

                    }

                }
                if (ballRect13.right >= ballRect4.left &&
                    ballRect13.left <= ballRect4.right &&
                    ballRect13.bottom >= ballRect4.top &&
                    ballRect13.top <= ballRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle13 = (angle + 180) % 360;



                    if (speed13 > 0) {

                        speed13 = (speed - 1);

                    }

                }
                if (ballRect13.right >= ballRect5.left &&
                    ballRect13.left <= ballRect5.right &&
                    ballRect13.bottom >= ballRect5.top &&
                    ballRect13.top <= ballRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle13 = (angle + 180) % 360;



                    if (speed13 > 0) {

                        speed13 = (speed - 1);

                    }

                }
                if (ballRect13.right >= ballRect6.left &&
                    ballRect13.left <= ballRect6.right &&
                    ballRect13.bottom >= ballRect6.top &&
                    ballRect13.top <= ballRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle13 = (angle + 180) % 360;



                    if (speed13 > 0) {

                        speed13 = (speed - 1);

                    }

                }
                if (ballRect13.right >= ballRect7.left &&
                    ballRect13.left <= ballRect7.right &&
                    ballRect13.bottom >= ballRect7.top &&
                    ballRect13.top <= ballRect7.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle13 = (angle + 180) % 360;



                    if (speed13 > 0) {

                        speed13 = (speed - 1);

                    }

                }
                if (ballRect13.right >= eightRect.left &&
                    ballRect13.left <= eightRect.right &&
                    ballRect13.bottom >= eightRect.top &&
                    ballRect13.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle13 = (angle + 180) % 360;



                    if (speed13 > 0) {

                        speed13 = (speed - 1);

                    }

                }
                if (ballRect13.right >= ballRect9.left &&
                    ballRect13.left <= ballRect9.right &&
                    ballRect13.bottom >= ballRect9.top &&
                    ballRect13.top <= ballRect9.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle13 = (angle + 180) % 360;



                    if (speed13 > 0) {

                        speed13 = (speed - 1);

                    }

                }
                if (ballRect13.right >= ballRect10.left &&
                    ballRect13.left <= ballRect10.right &&
                    ballRect13.bottom >= ballRect10.top &&
                    ballRect13.top <= ballRect10.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle13 = (angle + 180) % 360;



                    if (speed13 > 0) {

                        speed13 = (speed - 1);

                    }

                }
                if (ballRect13.right >= ballRect11.left &&
                    ballRect13.left <= ballRect11.right &&
                    ballRect13.bottom >= ballRect11.top &&
                    ballRect13.top <= ballRect11.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle13 = (angle + 180) % 360;



                    if (speed13 > 0) {

                        speed13 = (speed- 1);

                    }

                }
                if (ballRect13.right >= ballRect12.left &&
                    ballRect13.left <= ballRect12.right &&
                    ballRect13.bottom >= ballRect12.top &&
                    ballRect13.top <= ballRect12.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle13 = (angle + 180) % 360;



                    if (speed13 > 0) {

                        speed13 = (speed - 1);

                    }

                }
                if (ballRect13.right >= wallRect1.left &&
                    ballRect13.left <= wallRect1.right &&
                    ballRect13.bottom >= wallRect1.top &&
                    ballRect13.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle13 = 200 - angle13;
                    if (speed13 > 0) {

                        speed13 -= 1;

                    }

                }

                if (ballRect13.right >= wallRect2.left &&
                    ballRect13.left <= wallRect2.right &&
                    ballRect13.bottom >= wallRect2.top &&
                    ballRect13.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle13 = 250 + angle13;
                    if (speed13 > 0) {

                        speed13 -= 1;

                    }

                }

                if (ballRect13.right >= wallRect3.left &&
                    ballRect13.left <= wallRect3.right &&
                    ballRect13.bottom >= wallRect3.top &&
                    ballRect13.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle13 = 250 + angle13;
                    if (speed13 > 0) {

                        speed13 -= 1;

                    }

                }

                if (ballRect13.right >= wallRect4.left &&
                    ballRect13.left <= wallRect4.right &&
                    ballRect13.bottom >= wallRect4.top &&
                    ballRect13.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle13 = 250 + angle13;
                    if (speed13 > 0) {

                        speed13 -= 1;

                    }

                }

                if (ballRect13.right >= wallRect5.left &&
                    ballRect13.left <= wallRect5.right &&
                    ballRect13.bottom >= wallRect5.top &&
                    ballRect13.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle13 = 250 + angle13;
                    if (speed13 > 0) {

                        speed13 -= 1;

                    }

                }

                if (ballRect13.right >= wallRect6.left &&
                    ballRect13.left <= wallRect6.right &&
                    ballRect13.bottom >= wallRect6.top &&
                    ballRect13.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle13 = 200 - angle13;
                    if (speed13 > 0) {

                        speed13 -= 1;

                    }

                }
                ///////////////////////////////////////////////////////////////////////////////b14
                if (ballRect14.right >= ballRect.left &&
                    ballRect14.left <= ballRect.right &&
                    ballRect14.bottom >= ballRect.top &&
                    ballRect14.top <= ballRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle14 = (angle + 180) % 360;



                    if (speed14 > 0) {

                        speed14= (speed - 1);

                    }


                }
                if (ballRect14.right >= ballRect1.left &&
                    ballRect14.left <= ballRect1.right &&
                    ballRect14.bottom >= ballRect1.top &&
                    ballRect14.top <= ballRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle14 = (angle + 180) % 360;



                    if (speed14 > 0) {

                        speed14 = (speed - 1);

                    }

                }
                if (ballRect14.right >= ballRect2.left &&
                    ballRect14.left <= ballRect2.right &&
                    ballRect14.bottom >= ballRect2.top &&
                    ballRect14.top <= ballRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle14 = (angle + 180) % 360;



                    if (speed14 > 0) {

                        speed14 = (speed - 1);

                    }

                }
                if (ballRect14.right >= ballRect3.left &&
                    ballRect14.left <= ballRect3.right &&
                    ballRect14.bottom >= ballRect3.top &&
                    ballRect14.top <= ballRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle14 = (angle + 180) % 360;



                    if (speed14 > 0) {

                        speed14 = (speed - 1);

                    }

                }
                if (ballRect14.right >= ballRect4.left &&
                    ballRect14.left <= ballRect4.right &&
                    ballRect14.bottom >= ballRect4.top &&
                    ballRect14.top <= ballRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle14 = (angle + 180) % 360;



                    if (speed14 > 0) {

                        speed14 = (speed - 1);

                    }
                }
                if (ballRect14.right >= ballRect5.left &&
                    ballRect14.left <= ballRect5.right &&
                    ballRect14.bottom >= ballRect5.top &&
                    ballRect14.top <= ballRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle14 = (angle + 180) % 360;



                    if (speed14 > 0) {

                        speed14 = (speed- 1);

                    }

                }
                if (ballRect14.right >= ballRect6.left &&
                    ballRect14.left <= ballRect6.right &&
                    ballRect14.bottom >= ballRect6.top &&
                    ballRect14.top <= ballRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle14 = (angle+ 180) % 360;



                    if (speed14 > 0) {

                        speed14 = (speed - 1);

                    }

                }
                if (ballRect14.right >= ballRect7.left &&
                    ballRect14.left <= ballRect7.right &&
                    ballRect14.bottom >= ballRect7.top &&
                    ballRect14.top <= ballRect7.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle14 = (angle + 180) % 360;



                    if (speed14 > 0) {

                        speed14 = (speed - 1);

                    }
                }
                if (ballRect14.right >= eightRect.left &&
                    ballRect14.left <= eightRect.right &&
                    ballRect14.bottom >= eightRect.top &&
                    ballRect14.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle14 = (angle + 180) % 360;



                    if (speed14 > 0) {

                        speed14 = (speed - 1);

                    }

                }
                if (ballRect14.right >= ballRect9.left &&
                    ballRect14.left <= ballRect9.right &&
                    ballRect14.bottom >= ballRect9.top &&
                    ballRect14.top <= ballRect9.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle14 = (angle + 180) % 360;



                    if (speed14 > 0) {

                        speed14 = (speed - 1);

                    }

                }
                if (ballRect14.right >= ballRect10.left &&
                    ballRect14.left <= ballRect10.right &&
                    ballRect14.bottom >= ballRect10.top &&
                    ballRect14.top <= ballRect10.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle14 = (angle + 180) % 360;



                    if (speed14 > 0) {

                        speed14 = (speed- 1);

                    }

                }
                if (ballRect14.right >= ballRect11.left &&
                    ballRect14.left <= ballRect11.right &&
                    ballRect14.bottom >= ballRect11.top &&
                    ballRect14.top <= ballRect11.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle14 = (angle + 180) % 360;



                    if (speed14 > 0) {

                        speed14 = (speed - 1);

                    }

                }
                if (ballRect14.right >= ballRect12.left &&
                    ballRect14.left <= ballRect12.right &&
                    ballRect14.bottom >= ballRect12.top &&
                    ballRect14.top <= ballRect12.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle14 = (angle + 180) % 360;



                    if (speed14 > 0) {

                        speed14 = (speed- 1);

                    }

                }
                if (ballRect14.right >= ballRect13.left &&
                    ballRect14.left <= ballRect13.right &&
                    ballRect14.bottom >= ballRect13.top &&
                    ballRect14.top <= ballRect13.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle14 = (angle + 180) % 360;



                    if (speed14 > 0) {

                        speed14 = (speed - 1);

                    }
                }
                if (ballRect14.right >= wallRect1.left &&
                    ballRect14.left <= wallRect1.right &&
                    ballRect14.bottom >= wallRect1.top &&
                    ballRect14.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle14 = 200 - angle14;
                    if (speed14 > 0) {

                        speed14 -= 1;

                    }

                }

                if (ballRect14.right >= wallRect2.left &&
                    ballRect14.left <= wallRect2.right &&
                    ballRect14.bottom >= wallRect2.top &&
                    ballRect14.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle14 = 250 + angle14;
                    if (speed14 > 0) {

                        speed14 -= 1;

                    }

                }

                if (ballRect14.right >= wallRect3.left &&
                    ballRect14.left <= wallRect3.right &&
                    ballRect14.bottom >= wallRect3.top &&
                    ballRect14.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle14 = 250 + angle14;
                    if (speed14 > 0) {

                        speed14 -= 1;

                    }

                }

                if (ballRect14.right >= wallRect4.left &&
                    ballRect14.left <= wallRect4.right &&
                    ballRect14.bottom >= wallRect4.top &&
                    ballRect14.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle14 = 250 + angle14;

                    if (speed14 > 0) {

                        speed14 -= 1;

                    }
                }

                if (ballRect14.right >= wallRect5.left &&
                    ballRect14.left <= wallRect5.right &&
                    ballRect14.bottom >= wallRect5.top &&
                    ballRect14.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle14 = 250 + angle14;
                    if (speed14 > 0) {

                        speed14 -= 1;

                    }

                }

                if (ballRect14.right >= wallRect6.left &&
                    ballRect14.left <= wallRect6.right &&
                    ballRect14.bottom >= wallRect6.top &&
                    ballRect14.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle14 = 200 - angle14;
                    if (speed14 > 0) {

                        speed14 -= 1;

                    }

                }
                ///////////////////////////////////////////////////////////////////////////////b15
                if (ballRect15.right >= ballRect.left &&
                    ballRect15.left <= ballRect.right &&
                    ballRect15.bottom >= ballRect.top &&
                    ballRect15.top <= ballRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = (angle + 180) % 360;



                    if (speed15 > 0) {

                        speed15 = (speed - 1);

                    }

                }
                if (ballRect15.right >= ballRect1.left &&
                    ballRect15.left <= ballRect1.right &&
                    ballRect15.bottom >= ballRect1.top &&
                    ballRect15.top <= ballRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = (angle + 180) % 360;



                    if (speed15 > 0) {

                        speed15 = (speed - 1);

                    }

                }
                if (ballRect15.right >= ballRect2.left &&
                    ballRect15.left <= ballRect2.right &&
                    ballRect15.bottom >= ballRect2.top &&
                    ballRect15.top <= ballRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = (angle + 180) % 360;



                    if (speed15 > 0) {

                        speed15 = (speed - 1);

                    }

                }
                if (ballRect15.right >= ballRect3.left &&
                    ballRect15.left <= ballRect3.right &&
                    ballRect15.bottom >= ballRect3.top &&
                    ballRect15.top <= ballRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = (angle + 180) % 360;



                    if (speed15 > 0) {

                        speed15 = (speed - 1);

                    }

                }
                if (ballRect15.right >= ballRect4.left &&
                    ballRect15.left <= ballRect4.right &&
                    ballRect15.bottom >= ballRect4.top &&
                    ballRect15.top <= ballRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = (angle + 180) % 360;



                    if (speed15 > 0) {

                        speed15 = (speed - 1);

                    }

                }
                if (ballRect15.right >= ballRect5.left &&
                    ballRect15.left <= ballRect5.right &&
                    ballRect15.bottom >= ballRect5.top &&
                    ballRect15.top <= ballRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = (angle + 180) % 360;



                    if (speed15 > 0) {

                        speed15 = (speed - 1);

                    }

                }
                if (ballRect15.right >= ballRect6.left &&
                    ballRect15.left <= ballRect6.right &&
                    ballRect15.bottom >= ballRect6.top &&
                    ballRect15.top <= ballRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = (angle + 180) % 360;



                    if (speed15 > 0) {

                        speed15 = (speed- 1);

                    }

                }
                if (ballRect15.right >= ballRect7.left &&
                    ballRect15.left <= ballRect7.right &&
                    ballRect15.bottom >= ballRect7.top &&
                    ballRect15.top <= ballRect7.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = (angle + 180) % 360;



                    if (speed15 > 0) {

                        speed15 = (speed- 1);

                    }

                }
                if (ballRect15.right >= eightRect.left &&
                    ballRect15.left <= eightRect.right &&
                    ballRect15.bottom >= eightRect.top &&
                    ballRect15.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = (angle + 180) % 360;



                    if (speed15 > 0) {

                        speed15 = (speed - 1);

                    }

                }
                if (ballRect15.right >= ballRect9.left &&
                    ballRect15.left <= ballRect9.right &&
                    ballRect15.bottom >= ballRect9.top &&
                    ballRect15.top <= ballRect9.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = (angle + 180) % 360;



                    if (speed15 > 0) {

                        speed15 = (speed- 1);

                    }

                }
                if (ballRect15.right >= ballRect10.left &&
                    ballRect15.left <= ballRect10.right &&
                    ballRect15.bottom >= ballRect10.top &&
                    ballRect15.top <= ballRect10.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = (angle + 180) % 360;



                    if (speed15 > 0) {

                        speed15 = (speed - 1);

                    }

                }
                if (ballRect15.right >= ballRect11.left &&
                    ballRect15.left <= ballRect11.right &&
                    ballRect15.bottom >= ballRect11.top &&
                    ballRect15.top <= ballRect11.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = (angle + 180) % 360;



                    if (speed15 > 0) {

                        speed15 = (speed - 1);

                    }

                }
                if (ballRect15.right >= ballRect12.left &&
                    ballRect15.left <= ballRect12.right &&
                    ballRect15.bottom >= ballRect12.top &&
                    ballRect15.top <= ballRect12.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = (angle + 180) % 360;



                    if (speed15 > 0) {

                        speed15 = (speed - 1);

                    }

                }
                if (ballRect15.right >= ballRect13.left &&
                    ballRect15.left <= ballRect13.right &&
                    ballRect15.bottom >= ballRect13.top &&
                    ballRect15.top <= ballRect13.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = (angle + 180) % 360;



                    if (speed15 > 0) {

                        speed15 = (speed- 1);

                    }

                }
                if (ballRect15.right >= ballRect14.left &&
                    ballRect15.left <= ballRect14.right &&
                    ballRect15.bottom >= ballRect14.top &&
                    ballRect15.top <= ballRect14.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = (angle + 180) % 360;



                    if (speed15 > 0) {

                        speed15 = (speed - 1);

                    }

                }
                if (ballRect15.right >= wallRect1.left &&
                    ballRect15.left <= wallRect1.right &&
                    ballRect15.bottom >= wallRect1.top &&
                    ballRect15.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle15 = 200 - angle15;
                    if (speed15 > 0) {

                        speed15 -= 1;

                    }

                }

                if (ballRect15.right >= wallRect2.left &&
                    ballRect15.left <= wallRect2.right &&
                    ballRect15.bottom >= wallRect2.top &&
                    ballRect15.top <= wallRect2.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle15 = 250 + angle15;

                    if (speed15 > 0) {

                        speed15 -= 1;

                    }
                }

                if (ballRect15.right >= wallRect3.left &&
                    ballRect15.left <= wallRect3.right &&
                    ballRect15.bottom >= wallRect3.top &&
                    ballRect15.top <= wallRect3.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle15 = 250 + angle15;

                    if (speed15 > 0) {

                        speed15 -= 1;

                    }
                }

                if (ballRect15.right >= wallRect4.left &&
                    ballRect15.left <= wallRect4.right &&
                    ballRect15.bottom >= wallRect4.top &&
                    ballRect15.top <= wallRect4.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle15 = 250 + angle15;

                    if (speed15 > 0) {

                        speed15 -= 1;

                    }
                }

                if (ballRect15.right >= wallRect5.left &&
                    ballRect15.left <= wallRect5.right &&
                    ballRect15.bottom >= wallRect5.top &&
                    ballRect15.top <= wallRect5.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ

                    angle15 = 250 + angle15;

                    if (speed15 > 0) {

                        speed15 -= 1;

                    }
                }

                if (ballRect15.right >= wallRect6.left &&
                    ballRect15.left <= wallRect6.right &&
                    ballRect15.bottom >= wallRect6.top &&
                    ballRect15.top <= wallRect6.bottom) {

                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle15 = 200 - angle15;
                    if (speed15 > 0) {

                        speed15 -= 1;

                    }

                }

            }
            // حرکت توپ

            function checkCollision2(ball, eball11, eball22, eball33, eball44, eball55, eball66, eball77, eball99, eball1010, eball1111, eball1212, eball1313, eball1414, eball1515, eeightt, wall11, wall22, wall33, wall44, wall55, wall66) {
                var ballRect = ball.getBoundingClientRect();
                var ballRect1 = eball11.getBoundingClientRect();
                var ballRect2 = eball22.getBoundingClientRect();
                var ballRect3 = eball33.getBoundingClientRect();
                var ballRect4 = eball44.getBoundingClientRect();
                var ballRect5 = eball55.getBoundingClientRect();
                var ballRect6 = eball66.getBoundingClientRect();
                var ballRect7 = eball77.getBoundingClientRect();

                var ballRect9 = eball99.getBoundingClientRect();
                var ballRect10 = eball1010.getBoundingClientRect();
                var ballRect11 = eball1111.getBoundingClientRect();
                var ballRect12 = eball1212.getBoundingClientRect();
                var ballRect13 = eball1313.getBoundingClientRect();
                var ballRect14 = eball1414.getBoundingClientRect();
                var ballRect15 = eball1515.getBoundingClientRect();
                var eightRect = eeightt.getBoundingClientRect();

                var wallRect1 = wall11.getBoundingClientRect();
                var wallRect2 = wall22.getBoundingClientRect();
                var wallRect3 = wall33.getBoundingClientRect();
                var wallRect4 = wall44.getBoundingClientRect();
                var wallRect5 = wall55.getBoundingClientRect();
                var wallRect6 = wall66.getBoundingClientRect();
                $("#echob").hide();
                /////////////////////////////////////////////////////////w1
                if (ballRect.right >= wallRect1.left &&
                    ballRect.left <= wallRect1.right &&
                    ballRect.bottom >= wallRect1.top &&
                    ballRect.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(bwhite, angel);

                }
                if (ballRect1.right >= wallRect1.left &&
                    ballRect1.left <= wallRect1.right &&
                    ballRect1.bottom >= wallRect1.top &&
                    ballRect1.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball1, angel);
                }
                if (ballRect2.right >= wallRect1.left &&
                    ballRect2.left <= wallRect1.right &&
                    ballRect2.bottom >= wallRect1.top &&
                    ballRect2.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball2, angel);
                }
                if (ballRect3.right >= wallRect1.left &&
                    ballRect3.left <= wallRect1.right &&
                    ballRect3.bottom >= wallRect1.top &&
                    ballRect3.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball3, angel);
                }
                if (ballRect4.right >= wallRect1.left &&
                    ballRect4.left <= wallRect1.right &&
                    ballRect4.bottom >= wallRect1.top &&
                    ballRect4.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball4, angel);
                }
                if (ballRect5.right >= wallRect1.left &&
                    ballRect5.left <= wallRect1.right &&
                    ballRect5.bottom >= wallRect1.top &&
                    ballRect5.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball5, angel);
                }
                if (ballRect6.right >= wallRect1.left &&
                    ballRect6.left <= wallRect1.right &&
                    ballRect6.bottom >= wallRect1.top &&
                    ballRect6.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball6, angel);
                }
                if (ballRect7.right >= wallRect1.left &&
                    ballRect7.left <= wallRect1.right &&
                    ballRect7.bottom >= wallRect1.top &&
                    ballRect7.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball7, angel);
                }
                if (ballRect9.right >= wallRect1.left &&
                    ballRect9.left <= wallRect1.right &&
                    ballRect9.bottom >= wallRect1.top &&
                    ballRect9.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball9, angel);
                }
                if (ballRect10.right >= wallRect1.left &&
                    ballRect10.left <= wallRect1.right &&
                    ballRect10.bottom >= wallRect1.top &&
                    ballRect10.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball10, angel);
                }
                if (ballRect11.right >= wallRect1.left &&
                    ballRect11.left <= wallRect1.right &&
                    ballRect11.bottom >= wallRect1.top &&
                    ballRect11.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball11, angel);
                }
                if (ballRect12.right >= wallRect1.left &&
                    ballRect12.left <= wallRect1.right &&
                    ballRect12.bottom >= wallRect1.top &&
                    ballRect12.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball12, angel);
                }
                if (ballRect13.right >= wallRect1.left &&
                    ballRect13.left <= wallRect1.right &&
                    ballRect13.bottom >= wallRect1.top &&
                    ballRect13.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball13, angel);
                }
                if (ballRect14.right >= wallRect1.left &&
                    ballRect14.left <= wallRect1.right &&
                    ballRect14.bottom >= wallRect1.top &&
                    ballRect14.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball14, angel);
                }
                if (ballRect15.right >= wallRect1.left &&
                    ballRect15.left <= wallRect1.right &&
                    ballRect15.bottom >= wallRect1.top &&
                    ballRect15.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball15, angel);
                }
                if (eightRect.right >= wallRect1.left &&
                    eightRect.left <= wallRect1.right &&
                    eightRect.bottom >= wallRect1.top &&
                    eightRect.top <= wallRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eeight, angel);
                }
                /////////////////////////////////////////////////////////////////
                /////////////////////////////////////////////////////////w2
                if (ballRect.right >= wallRect2.left &&
                    ballRect.left <= wallRect2.right &&
                    ballRect.bottom >= wallRect2.top &&
                    ballRect.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(bwhite, angel);

                }
                if (ballRect1.right >= wallRect2.left &&
                    ballRect1.left <= wallRect2.right &&
                    ballRect1.bottom >= wallRect2.top &&
                    ballRect1.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball1, angel);
                }
                if (ballRect2.right >= wallRect2.left &&
                    ballRect2.left <= wallRect2.right &&
                    ballRect2.bottom >= wallRect2.top &&
                    ballRect2.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball2, angel);
                }
                if (ballRect3.right >= wallRect2.left &&
                    ballRect3.left <= wallRect2.right &&
                    ballRect3.bottom >= wallRect2.top &&
                    ballRect3.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball3, angel);
                }
                if (ballRect4.right >= wallRect2.left &&
                    ballRect4.left <= wallRect2.right &&
                    ballRect4.bottom >= wallRect2.top &&
                    ballRect4.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball4, angel);
                }
                if (ballRect5.right >= wallRect2.left &&
                    ballRect5.left <= wallRect2.right &&
                    ballRect5.bottom >= wallRect2.top &&
                    ballRect5.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball5, angel);
                }
                if (ballRect6.right >= wallRect2.left &&
                    ballRect6.left <= wallRect2.right &&
                    ballRect6.bottom >= wallRect2.top &&
                    ballRect6.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball6, angel);
                }
                if (ballRect7.right >= wallRect2.left &&
                    ballRect7.left <= wallRect2.right &&
                    ballRect7.bottom >= wallRect2.top &&
                    ballRect7.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball7, angel);
                }
                if (ballRect9.right >= wallRect2.left &&
                    ballRect9.left <= wallRect2.right &&
                    ballRect9.bottom >= wallRect2.top &&
                    ballRect9.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball9, angel);
                }
                if (ballRect10.right >= wallRect2.left &&
                    ballRect10.left <= wallRect2.right &&
                    ballRect10.bottom >= wallRect2.top &&
                    ballRect10.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball10, angel);
                }
                if (ballRect11.right >= wallRect2.left &&
                    ballRect11.left <= wallRect2.right &&
                    ballRect11.bottom >= wallRect2.top &&
                    ballRect11.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball11, angel);
                }
                if (ballRect12.right >= wallRect2.left &&
                    ballRect12.left <= wallRect2.right &&
                    ballRect12.bottom >= wallRect2.top &&
                    ballRect12.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball12, angel);
                }
                if (ballRect13.right >= wallRect2.left &&
                    ballRect13.left <= wallRect2.right &&
                    ballRect13.bottom >= wallRect2.top &&
                    ballRect13.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball13, angel);
                }
                if (ballRect14.right >= wallRect2.left &&
                    ballRect14.left <= wallRect2.right &&
                    ballRect14.bottom >= wallRect2.top &&
                    ballRect14.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball14, angel);
                }
                if (ballRect15.right >= wallRect2.left &&
                    ballRect15.left <= wallRect2.right &&
                    ballRect15.bottom >= wallRect2.top &&
                    ballRect15.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball15, angel);
                }
                if (eightRect.right >= wallRect2.left &&
                    eightRect.left <= wallRect2.right &&
                    eightRect.bottom >= wallRect2.top &&
                    eightRect.top <= wallRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eeight, angel);
                }
                /////////////////////////////////////////////////////////////////
                /////////////////////////////////////////////////////////w3
                if (ballRect.right >= wallRect3.left &&
                    ballRect.left <= wallRect3.right &&
                    ballRect.bottom >= wallRect3.top &&
                    ballRect.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(bwhite, angel);

                }
                if (ballRect1.right >= wallRect3.left &&
                    ballRect1.left <= wallRect3.right &&
                    ballRect1.bottom >= wallRect3.top &&
                    ballRect1.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball1, angel);
                }
                if (ballRect2.right >= wallRect3.left &&
                    ballRect2.left <= wallRect3.right &&
                    ballRect2.bottom >= wallRect3.top &&
                    ballRect2.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball2, angel);
                }
                if (ballRect3.right >= wallRect3.left &&
                    ballRect3.left <= wallRect3.right &&
                    ballRect3.bottom >= wallRect3.top &&
                    ballRect3.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball3, angel);
                }
                if (ballRect4.right >= wallRect3.left &&
                    ballRect4.left <= wallRect3.right &&
                    ballRect4.bottom >= wallRect3.top &&
                    ballRect4.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball4, angel);
                }
                if (ballRect5.right >= wallRect3.left &&
                    ballRect5.left <= wallRect3.right &&
                    ballRect5.bottom >= wallRect3.top &&
                    ballRect5.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball5, angel);
                }
                if (ballRect6.right >= wallRect3.left &&
                    ballRect6.left <= wallRect3.right &&
                    ballRect6.bottom >= wallRect3.top &&
                    ballRect6.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball6, angel);
                }
                if (ballRect7.right >= wallRect3.left &&
                    ballRect7.left <= wallRect3.right &&
                    ballRect7.bottom >= wallRect3.top &&
                    ballRect7.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball7, angel);
                }
                if (ballRect9.right >= wallRect3.left &&
                    ballRect9.left <= wallRect3.right &&
                    ballRect9.bottom >= wallRect3.top &&
                    ballRect9.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball9, angel);
                }
                if (ballRect10.right >= wallRect3.left &&
                    ballRect10.left <= wallRect3.right &&
                    ballRect10.bottom >= wallRect3.top &&
                    ballRect10.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball10, angel);
                }
                if (ballRect11.right >= wallRect3.left &&
                    ballRect11.left <= wallRect3.right &&
                    ballRect11.bottom >= wallRect3.top &&
                    ballRect11.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball11, angel);
                }
                if (ballRect12.right >= wallRect3.left &&
                    ballRect12.left <= wallRect3.right &&
                    ballRect12.bottom >= wallRect3.top &&
                    ballRect12.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball12, angel);
                }
                if (ballRect13.right >= wallRect3.left &&
                    ballRect13.left <= wallRect3.right &&
                    ballRect13.bottom >= wallRect3.top &&
                    ballRect13.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball13, angel);
                }
                if (ballRect14.right >= wallRect3.left &&
                    ballRect14.left <= wallRect3.right &&
                    ballRect14.bottom >= wallRect3.top &&
                    ballRect14.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball14, angel);
                }
                if (ballRect15.right >= wallRect3.left &&
                    ballRect15.left <= wallRect3.right &&
                    ballRect15.bottom >= wallRect3.top &&
                    ballRect15.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball15, angel);
                }
                if (eightRect.right >= wallRect3.left &&
                    eightRect.left <= wallRect3.right &&
                    eightRect.bottom >= wallRect3.top &&
                    eightRect.top <= wallRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eeight, angel);
                }
                /////////////////////////////////////////////////////////////////
                /////////////////////////////////////////////////////////w4
                if (ballRect.right >= wallRect4.left &&
                    ballRect.left <= wallRect4.right &&
                    ballRect.bottom >= wallRect4.top &&
                    ballRect.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(bwhite, angel);

                }
                if (ballRect1.right >= wallRect4.left &&
                    ballRect1.left <= wallRect4.right &&
                    ballRect1.bottom >= wallRect4.top &&
                    ballRect1.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball1, angel);
                }
                if (ballRect2.right >= wallRect4.left &&
                    ballRect2.left <= wallRect4.right &&
                    ballRect2.bottom >= wallRect4.top &&
                    ballRect2.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball2, angel);
                }
                if (ballRect3.right >= wallRect4.left &&
                    ballRect3.left <= wallRect4.right &&
                    ballRect3.bottom >= wallRect4.top &&
                    ballRect3.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball3, angel);
                }
                if (ballRect4.right >= wallRect4.left &&
                    ballRect4.left <= wallRect4.right &&
                    ballRect4.bottom >= wallRect4.top &&
                    ballRect4.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball4, angel);
                }
                if (ballRect5.right >= wallRect4.left &&
                    ballRect5.left <= wallRect4.right &&
                    ballRect5.bottom >= wallRect4.top &&
                    ballRect5.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball5, angel);
                }
                if (ballRect6.right >= wallRect4.left &&
                    ballRect6.left <= wallRect4.right &&
                    ballRect6.bottom >= wallRect4.top &&
                    ballRect6.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball6, angel);
                }
                if (ballRect7.right >= wallRect4.left &&
                    ballRect7.left <= wallRect4.right &&
                    ballRect7.bottom >= wallRect4.top &&
                    ballRect7.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball7, angel);
                }
                if (ballRect9.right >= wallRect4.left &&
                    ballRect9.left <= wallRect4.right &&
                    ballRect9.bottom >= wallRect4.top &&
                    ballRect9.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball9, angel);
                }
                if (ballRect10.right >= wallRect4.left &&
                    ballRect10.left <= wallRect4.right &&
                    ballRect10.bottom >= wallRect4.top &&
                    ballRect10.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball10, angel);
                }
                if (ballRect11.right >= wallRect4.left &&
                    ballRect11.left <= wallRect4.right &&
                    ballRect11.bottom >= wallRect4.top &&
                    ballRect11.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball11, angel);
                }
                if (ballRect12.right >= wallRect4.left &&
                    ballRect12.left <= wallRect4.right &&
                    ballRect12.bottom >= wallRect4.top &&
                    ballRect12.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball12, angel);
                }
                if (ballRect13.right >= wallRect4.left &&
                    ballRect13.left <= wallRect4.right &&
                    ballRect13.bottom >= wallRect4.top &&
                    ballRect13.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball13, angel);
                }
                if (ballRect14.right >= wallRect4.left &&
                    ballRect14.left <= wallRect4.right &&
                    ballRect14.bottom >= wallRect4.top &&
                    ballRect14.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball14, angel);
                }
                if (ballRect15.right >= wallRect4.left &&
                    ballRect15.left <= wallRect4.right &&
                    ballRect15.bottom >= wallRect4.top &&
                    ballRect15.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball15, angel);
                }
                if (eightRect.right >= wallRect4.left &&
                    eightRect.left <= wallRect4.right &&
                    eightRect.bottom >= wallRect4.top &&
                    eightRect.top <= wallRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eeight, angel);
                }
                /////////////////////////////////////////////////////////////////
                /////////////////////////////////////////////////////////w5
                if (ballRect.right >= wallRect5.left &&
                    ballRect.left <= wallRect5.right &&
                    ballRect.bottom >= wallRect5.top &&
                    ballRect.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(bwhite, angel);

                }
                if (ballRect1.right >= wallRect5.left &&
                    ballRect1.left <= wallRect5.right &&
                    ballRect1.bottom >= wallRect5.top &&
                    ballRect1.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball1, angel);
                }
                if (ballRect2.right >= wallRect5.left &&
                    ballRect2.left <= wallRect5.right &&
                    ballRect2.bottom >= wallRect5.top &&
                    ballRect2.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball2, angel);
                }
                if (ballRect3.right >= wallRect5.left &&
                    ballRect3.left <= wallRect5.right &&
                    ballRect3.bottom >= wallRect5.top &&
                    ballRect3.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball3, angel);
                }
                if (ballRect4.right >= wallRect5.left &&
                    ballRect4.left <= wallRect5.right &&
                    ballRect4.bottom >= wallRect5.top &&
                    ballRect4.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball4, angel);
                }
                if (ballRect5.right >= wallRect5.left &&
                    ballRect5.left <= wallRect5.right &&
                    ballRect5.bottom >= wallRect5.top &&
                    ballRect5.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball5, angel);
                }
                if (ballRect6.right >= wallRect5.left &&
                    ballRect6.left <= wallRect5.right &&
                    ballRect6.bottom >= wallRect5.top &&
                    ballRect6.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball6, angel);
                }
                if (ballRect7.right >= wallRect5.left &&
                    ballRect7.left <= wallRect5.right &&
                    ballRect7.bottom >= wallRect5.top &&
                    ballRect7.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball7, angel);
                }
                if (ballRect9.right >= wallRect5.left &&
                    ballRect9.left <= wallRect5.right &&
                    ballRect9.bottom >= wallRect5.top &&
                    ballRect9.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball9, angel);
                }
                if (ballRect10.right >= wallRect5.left &&
                    ballRect10.left <= wallRect5.right &&
                    ballRect10.bottom >= wallRect5.top &&
                    ballRect10.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball10, angel);
                }
                if (ballRect11.right >= wallRect5.left &&
                    ballRect11.left <= wallRect5.right &&
                    ballRect11.bottom >= wallRect5.top &&
                    ballRect11.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball11, angel);
                }
                if (ballRect12.right >= wallRect5.left &&
                    ballRect12.left <= wallRect5.right &&
                    ballRect12.bottom >= wallRect5.top &&
                    ballRect12.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball12, angel);
                }
                if (ballRect13.right >= wallRect5.left &&
                    ballRect13.left <= wallRect5.right &&
                    ballRect13.bottom >= wallRect5.top &&
                    ballRect13.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball13, angel);
                }
                if (ballRect14.right >= wallRect5.left &&
                    ballRect14.left <= wallRect5.right &&
                    ballRect14.bottom >= wallRect5.top &&
                    ballRect14.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball14, angel);
                }
                if (ballRect15.right >= wallRect5.left &&
                    ballRect15.left <= wallRect5.right &&
                    ballRect15.bottom >= wallRect5.top &&
                    ballRect15.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball15, angel);
                }
                if (eightRect.right >= wallRect5.left &&
                    eightRect.left <= wallRect5.right &&
                    eightRect.bottom >= wallRect5.top &&
                    eightRect.top <= wallRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eeight, angel);
                }
                /////////////////////////////////////////////////////////////////
                /////////////////////////////////////////////////////////w6
                if (ballRect.right >= wallRect6.left &&
                    ballRect.left <= wallRect6.right &&
                    ballRect.bottom >= wallRect6.top &&
                    ballRect.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(bwhite, angel);

                }
                if (ballRect1.right >= wallRect6.left &&
                    ballRect1.left <= wallRect6.right &&
                    ballRect1.bottom >= wallRect6.top &&
                    ballRect1.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball1, angel);
                }
                if (ballRect2.right >= wallRect6.left &&
                    ballRect2.left <= wallRect6.right &&
                    ballRect2.bottom >= wallRect6.top &&
                    ballRect2.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball2, angel);
                }
                if (ballRect3.right >= wallRect6.left &&
                    ballRect3.left <= wallRect6.right &&
                    ballRect3.bottom >= wallRect6.top &&
                    ballRect3.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball3, angel);
                }
                if (ballRect4.right >= wallRect6.left &&
                    ballRect4.left <= wallRect6.right &&
                    ballRect4.bottom >= wallRect6.top &&
                    ballRect4.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball4, angel);
                }
                if (ballRect5.right >= wallRect6.left &&
                    ballRect5.left <= wallRect6.right &&
                    ballRect5.bottom >= wallRect6.top &&
                    ballRect5.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball5, angel);
                }
                if (ballRect6.right >= wallRect6.left &&
                    ballRect6.left <= wallRect6.right &&
                    ballRect6.bottom >= wallRect6.top &&
                    ballRect6.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball6, angel);
                }
                if (ballRect7.right >= wallRect6.left &&
                    ballRect7.left <= wallRect6.right &&
                    ballRect7.bottom >= wallRect6.top &&
                    ballRect7.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball7, angel);
                }
                if (ballRect9.right >= wallRect6.left &&
                    ballRect9.left <= wallRect6.right &&
                    ballRect9.bottom >= wallRect6.top &&
                    ballRect9.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball9, angel);
                }
                if (ballRect10.right >= wallRect6.left &&
                    ballRect10.left <= wallRect6.right &&
                    ballRect10.bottom >= wallRect6.top &&
                    ballRect10.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball10, angel);
                }
                if (ballRect11.right >= wallRect6.left &&
                    ballRect11.left <= wallRect6.right &&
                    ballRect11.bottom >= wallRect6.top &&
                    ballRect11.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball11, angel);
                }
                if (ballRect12.right >= wallRect6.left &&
                    ballRect12.left <= wallRect6.right &&
                    ballRect12.bottom >= wallRect6.top &&
                    ballRect12.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball12, angel);
                }
                if (ballRect13.right >= wallRect6.left &&
                    ballRect13.left <= wallRect6.right &&
                    ballRect13.bottom >= wallRect6.top &&
                    ballRect13.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball13, angel);
                }
                if (ballRect14.right >= wallRect6.left &&
                    ballRect14.left <= wallRect6.right &&
                    ballRect14.bottom >= wallRect6.top &&
                    ballRect14.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball14, angel);
                }
                if (ballRect15.right >= wallRect6.left &&
                    ballRect15.left <= wallRect6.right &&
                    ballRect15.bottom >= wallRect6.top &&
                    ballRect15.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eball15, angel);
                }
                if (eightRect.right >= wallRect6.left &&
                    eightRect.left <= wallRect6.right &&
                    eightRect.bottom >= wallRect6.top &&
                    eightRect.top <= wallRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 - angle;
                    moveBall(eeight, angel);
                }

                 =b1
                if (ballRect.right >= ballRect1.left &&
                    ballRect.left <= ballRect1.right &&
                    ballRect.bottom >= bballRect1allRect.top &&
                    ballRect.top <= ballRect1.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect.right >= ballRect2.left &&
                    ballRect.left <= ballRect2.right &&
                    ballRect.bottom >= ballRect2.top &&
                    ballRect.top <= ballRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect.right >= ballRect3.left &&
                    ballRect.left <= ballRect3.right &&
                    ballRect.bottom >= ballRect3.top &&
                    ballRect.top <= ballRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect.right >= ballRect4.left &&
                    ballRect.left <= ballRect4.right &&
                    ballRect.bottom >= ballRect4.top &&
                    ballRect.top <= ballRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect.right >= ballRect5.left &&
                    ballRect.left <= ballRect5.right &&
                    ballRect.bottom >= ballRect5.top &&
                    ballRect.top <= ballRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect.right >= ballRect6.left &&
                    ballRect.left <= ballRect6.right &&
                    ballRect.bottom >= ballRect6.top &&
                    ballRect.top <= ballRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect.right >= ballRect7.left &&
                    ballRect.left <= ballRect7.right &&
                    ballRect.bottom >= ballRect7.top &&
                    ballRect.top <= ballRect7.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect.right >= ballRect9.left &&
                    ballRect.left <= ballRect9.right &&
                    ballRect.bottom >= ballRect9.top &&
                    ballRect.top <= ballRect9.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect.right >= ballRect10.left &&
                    ballRect.left <= ballRect10.right &&
                    ballRect.bottom >= ballRect10.top &&
                    ballRect.top <= ballRect10.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect.right >= ballRect11.left &&
                    ballRect.left <= ballRect11.right &&
                    ballRect.bottom >= ballRect11.top &&
                    ballRect.top <= ballRect11.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect.right >= ballRect12.left &&
                    ballRect.left <= ballRect12.right &&
                    ballRect.bottom >= ballRect12.top &&
                    ballRect.top <= ballRect12.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect.right >= ballRect13.left &&
                    ballRect.left <= ballRect13.right &&
                    ballRect.bottom >= ballRect13.top &&
                    ballRect.top <= ballRect13.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect.right >= ballRect14.left &&
                    ballRect.left <= ballRect14.right &&
                    ballRect.bottom >= ballRect14.top &&
                    ballRect.top <= ballRect14.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect.right >= ballRect15.left &&
                    ballRect.left <= ballRect15.right &&
                    ballRect.bottom >= ballRect15.top &&
                    ballRect.top <= ballRect15.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect.right >= eightRect.left &&
                    ballRect.left <= eightRect.right &&
                    ballRect.bottom >= eightRect.top &&
                    ballRect.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
            ///////////////////////////////////////////////////////////////////////////////////b1

                if (ballRect1.right >= ballRect2.left &&
                    ballRect1.left <= ballRect2.right &&
                    ballRect1.bottom >= ballRect2.top &&
                    ballRect1.top <= ballRect2.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect1.right >= ballRect3.left &&
                    ballRect1.left <= ballRect3.right &&
                    ballRect1.bottom >= ballRect3.top &&
                    ballRect1.top <= ballRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect1.right >= ballRect4.left &&
                    ballRect1.left <= ballRect4.right &&
                    ballRect1.bottom >= ballRect4.top &&
                    ballRect1.top <= ballRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect1.right >= ballRect5.left &&
                    ballRect1.left <= ballRect5.right &&
                    ballRect1.bottom >= ballRect5.top &&
                    ballRect1.top <= ballRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect1.right >= ballRect6.left &&
                    ballRect1.left <= ballRect6.right &&
                    ballRect1.bottom >= ballRect6.top &&
                    ballRect1.top <= ballRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect1.right >= ballRect7.left &&
                    ballRect1.left <= ballRect7.right &&
                    ballRect1.bottom >= ballRect7.top &&
                    ballRect1.top <= ballRect7.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect1.right >= ballRect9.left &&
                    ballRect1.left <= ballRect9.right &&
                    ballRect1.bottom >= ballRect9.top &&
                    ballRect1.top <= ballRect9.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect1.right >= ballRect10.left &&
                    ballRect1.left <= ballRect10.right &&
                    ballRect1.bottom >= ballRect10.top &&
                    ballRect1.top <= ballRect10.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect1.right >= ballRect11.left &&
                    ballRect1.left <= ballRect11.right &&
                    ballRect1.bottom >= ballRect11.top &&
                    ballRect1.top <= ballRect11.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect1.right >= ballRect12.left &&
                    ballRect1.left <= ballRect12.right &&
                    ballRect1.bottom >= ballRect12.top &&
                    ballRect1.top <= ballRect12.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect1.right >= ballRect13.left &&
                    ballRect1.left <= ballRect13.right &&
                    ballRect1.bottom >= ballRect13.top &&
                    ballRect1.top <= ballRect13.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect1.right >= ballRect14.left &&
                    ballRect1.left <= ballRect14.right &&
                    ballRect1.bottom >= ballRect14.top &&
                    ballRect1.top <= ballRect14.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect1.right >= ballRect15.left &&
                    ballRect1.left <= ballRect15.right &&
                    ballRect1.bottom >= ballRect15.top &&
                    ballRect1.top <= ballRect15.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect1.right >= eightRect.left &&
                    ballRect1.left <= eightRect.right &&
                    ballRect1.bottom >= eightRect.top &&
                    ballRect1.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                ///////////////////////////////////////////////////////////////////////////////b2

                if (ballRect2.right >= ballRect3.left &&
                    ballRect2.left <= ballRect3.right &&
                    ballRect2.bottom >= ballRect3.top &&
                    ballRect2.top <= ballRect3.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect2.right >= ballRect4.left &&
                    ballRect2.left <= ballRect4.right &&
                    ballRect2.bottom >= ballRect4.top &&
                    ballRect2.top <= ballRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect2.right >= ballRect5.left &&
                    ballRect2.left <= ballRect5.right &&
                    ballRect2.bottom >= ballRect5.top &&
                    ballRect2.top <= ballRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect2.right >= ballRect6.left &&
                    ballRect2.left <= ballRect6.right &&
                    ballRect2.bottom >= ballRect6.top &&
                    ballRect2.top <= ballRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect2.right >= ballRect7.left &&
                    ballRect2.left <= ballRect7.right &&
                    ballRect2.bottom >= ballRect7.top &&
                    ballRect2.top <= ballRect7.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect2.right >= ballRect9.left &&
                    ballRect2.left <= ballRect9.right &&
                    ballRect2.bottom >= ballRect9.top &&
                    ballRect2.top <= ballRect9.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect2.right >= ballRect10.left &&
                    ballRect2.left <= ballRect10.right &&
                    ballRect2.bottom >= ballRect10.top &&
                    ballRect2.top <= ballRect10.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect2.right >= ballRect11.left &&
                    ballRect2.left <= ballRect11.right &&
                    ballRect2.bottom >= ballRect11.top &&
                    ballRect2.top <= ballRect11.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect2.right >= ballRect12.left &&
                    ballRect2.left <= ballRect12.right &&
                    ballRect2.bottom >= ballRect12.top &&
                    ballRect2.top <= ballRect12.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect2.right >= ballRect13.left &&
                    ballRect2.left <= ballRect13.right &&
                    ballRect2.bottom >= ballRect13.top &&
                    ballRect2.top <= ballRect13.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect2.right >= ballRect14.left &&
                    ballRect2.left <= ballRect14.right &&
                    ballRect2.bottom >= ballRect14.top &&
                    ballRect2.top <= ballRect14.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect2.right >= ballRect15.left &&
                    ballRect2.left <= ballRect15.right &&
                    ballRect2.bottom >= ballRect15.top &&
                    ballRect2.top <= ballRect15.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect2.right >= eightRect.left &&
                    ballRect2.left <= eightRect.right &&
                    ballRect2.bottom >= eightRect.top &&
                    ballRect2.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                //////////////////////////////////////////////////////////////////////b3

                if (ballRect3.right >= ballRect4.left &&
                    ballRect3.left <= ballRect4.right &&
                    ballRect3.bottom >= ballRect4.top &&
                    ballRect3.top <= ballRect4.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect3.right >= ballRect5.left &&
                    ballRect3.left <= ballRect5.right &&
                    ballRect3.bottom >= ballRect5.top &&
                    ballRect3.top <= ballRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect3.right >= ballRect6.left &&
                    ballRect3.left <= ballRect6.right &&
                    ballRect3.bottom >= ballRect6.top &&
                    ballRect3.top <= ballRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect3.right >= ballRect7.left &&
                    ballRect3.left <= ballRect7.right &&
                    ballRect3.bottom >= ballRect7.top &&
                    ballRect3.top <= ballRect7.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect3.right >= ballRect9.left &&
                    ballRect3.left <= ballRect9.right &&
                    ballRect3.bottom >= ballRect9.top &&
                    ballRect3.top <= ballRect9.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect3.right >= ballRect10.left &&
                    ballRect3.left <= ballRect10.right &&
                    ballRect3.bottom >= ballRect10.top &&
                    ballRect3.top <= ballRect10.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect3.right >= ballRect11.left &&
                    ballRect3.left <= ballRect11.right &&
                    ballRect3.bottom >= ballRect11.top &&
                    ballRect3.top <= ballRect11.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect3.right >= ballRect12.left &&
                    ballRect3.left <= ballRect12.right &&
                    ballRect3.bottom >= ballRect12.top &&
                    ballRect3.top <= ballRect12.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect3.right >= ballRect13.left &&
                    ballRect3.left <= ballRect13.right &&
                    ballRect3.bottom >= ballRect13.top &&
                    ballRect3.top <= ballRect13.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect3.right >= ballRect14.left &&
                    ballRect3.left <= ballRect14.right &&
                    ballRect3.bottom >= ballRect14.top &&
                    ballRect3.top <= ballRect14.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect3.right >= ballRect15.left &&
                    ballRect3.left <= ballRect15.right &&
                    ballRect3.bottom >= ballRect15.top &&
                    ballRect3.top <= ballRect15.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect3.right >= eightRect.left &&
                    ballRect3.left <= eightRect.right &&
                    ballRect3.bottom >= eightRect.top &&
                    ballRect3.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                ///////////////////////////////////////////////////////////////b4

                if (ballRect4.right >= ballRect5.left &&
                    ballRect4.left <= ballRect5.right &&
                    ballRect4.bottom >= ballRect5.top &&
                    ballRect4.top <= ballRect5.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect4.right >= ballRect6.left &&
                    ballRect4.left <= ballRect6.right &&
                    ballRect4.bottom >= ballRect6.top &&
                    ballRect4.top <= ballRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect4.right >= ballRect7.left &&
                    ballRect4.left <= ballRect7.right &&
                    ballRect4.bottom >= ballRect7.top &&
                    ballRect4.top <= ballRect7.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect4.right >= ballRect9.left &&
                    ballRect4.left <= ballRect9.right &&
                    ballRect4.bottom >= ballRect9.top &&
                    ballRect4.top <= ballRect9.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect4.right >= ballRect10.left &&
                    ballRect4.left <= ballRect10.right &&
                    ballRect4.bottom >= ballRect10.top &&
                    ballRect4.top <= ballRect10.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect4.right >= ballRect11.left &&
                    ballRect4.left <= ballRect11.right &&
                    ballRect4.bottom >= ballRect11.top &&
                    ballRect4.top <= ballRect11.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect4.right >= ballRect12.left &&
                    ballRect4.left <= ballRect12.right &&
                    ballRect4.bottom >= ballRect12.top &&
                    ballRect4.top <= ballRect12.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect4.right >= ballRect13.left &&
                    ballRect4.left <= ballRect13.right &&
                    ballRect4.bottom >= ballRect13.top &&
                    ballRect4.top <= ballRect13.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect4.right >= ballRect14.left &&
                    ballRect4.left <= ballRect14.right &&
                    ballRect4.bottom >= ballRect14.top &&
                    ballRect4.top <= ballRect14.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect4.right >= ballRect15.left &&
                    ballRect4.left <= ballRect15.right &&
                    ballRect4.bottom >= ballRect15.top &&
                    ballRect4.top <= ballRect15.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect4.right >= eightRect.left &&
                    ballRect4.left <= eightRect.right &&
                    ballRect4.bottom >= eightRect.top &&
                    ballRect4.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                /////////////////////////////////////////////////////////////b5


                if (ballRect5.right >= ballRect6.left &&
                    ballRect5.left <= ballRect6.right &&
                    ballRect5.bottom >= ballRect6.top &&
                    ballRect5.top <= ballRect6.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect5.right >= ballRect7.left &&
                    ballRect5.left <= ballRect7.right &&
                    ballRect5.bottom >= ballRect7.top &&
                    ballRect5.top <= ballRect7.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect5.right >= ballRect9.left &&
                    ballRect5.left <= ballRect9.right &&
                    ballRect5.bottom >= ballRect9.top &&
                    ballRect5.top <= ballRect9.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect5.right >= ballRect10.left &&
                    ballRect5.left <= ballRect10.right &&
                    ballRect5.bottom >= ballRect10.top &&
                    ballRect5.top <= ballRect10.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect5.right >= ballRect11.left &&
                    ballRect5.left <= ballRect11.right &&
                    ballRect5.bottom >= ballRect11.top &&
                    ballRect5.top <= ballRect11.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect5.right >= ballRect12.left &&
                    ballRect5.left <= ballRect12.right &&
                    ballRect5.bottom >= ballRect12.top &&
                    ballRect5.top <= ballRect12.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect5.right >= ballRect13.left &&
                    ballRect5.left <= ballRect13.right &&
                    ballRect5.bottom >= ballRect13.top &&
                    ballRect5.top <= ballRect13.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect5.right >= ballRect14.left &&
                    ballRect5.left <= ballRect14.right &&
                    ballRect5.bottom >= ballRect14.top &&
                    ballRect5.top <= ballRect14.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect5.right >= ballRect15.left &&
                    ballRect5.left <= ballRect15.right &&
                    ballRect5.bottom >= ballRect15.top &&
                    ballRect5.top <= ballRect15.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect5.right >= eightRect.left &&
                    ballRect5.left <= eightRect.right &&
                    ballRect5.bottom >= eightRect.top &&
                    ballRect5.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                /////////////////////////////////////////////////////////////////b6

                if (ballRect6.right >= ballRect7.left &&
                    ballRect6.left <= ballRect7.right &&
                    ballRect6.bottom >= ballRect7.top &&
                    ballRect6.top <= ballRect7.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect6.right >= ballRect9.left &&
                    ballRect6.left <= ballRect9.right &&
                    ballRect6.bottom >= ballRect9.top &&
                    ballRect6.top <= ballRect9.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect6.right >= ballRect10.left &&
                    ballRect6.left <= ballRect10.right &&
                    ballRect6.bottom >= ballRect10.top &&
                    ballRect6.top <= ballRect10.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                    }
                    if (ballRect6.right >= ballRect11.left &&
                        ballRect6.left <= ballRect11.right &&
                        ballRect6.bottom >= ballRect11.top &&
                        ballRect6.top <= ballRect11.bottom) {
                        // برخورد اتفاق افتاده است، تغییر رنگ توپ
                        angle = 200 -angle;
                    }
                if (ballRect6.right >= ballRect12.left &&
                    ballRect6.left <= ballRect12.right &&
                    ballRect6.bottom >= ballRect12.top &&
                    ballRect6.top <= ballRect12.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect6.right >= ballRect13.left &&
                    ballRect6.left <= ballRect13.right &&
                    ballRect6.bottom >= ballRect13.top &&
                    ballRect6.top <= ballRect13.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect6.right >= ballRect14.left &&
                    ballRect6.left <= ballRect14.right &&
                    ballRect6.bottom >= ballRect14.top &&
                    ballRect6.top <= ballRect14.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect6.right >= ballRect15.left &&
                    ballRect6.left <= ballRect15.right &&
                    ballRect6.bottom >= ballRect15.top &&
                    ballRect6.top <= ballRect15.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect6.right >= eightRect.left &&
                    ballRect6.left <= eightRect.right &&
                    ballRect6.bottom >= eightRect.top &&
                    ballRect6.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                /////////////////////////////////////////////////////////////////b7

                if (ballRect7.right >= ballRect9.left &&
                    ballRect7.left <= ballRect9.right &&
                    ballRect7.bottom >= ballRect9.top &&
                    ballRect7.top <= ballRect9.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect7.right >= ballRect10.left &&
                    ballRect7.left <= ballRect10.right &&
                    ballRect7.bottom >= ballRect10.top &&
                    ballRect7.top <= ballRect10.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                    }
                    if (ballRect7.right >= ballRect11.left &&
                        ballRect7.left <= ballRect11.right &&
                        ballRect7.bottom >= ballRect11.top &&
                        ballRect7.top <= ballRect11.bottom) {
                        // برخورد اتفاق افتاده است، تغییر رنگ توپ
                        angle = 200 -angle;
                    }
                if (ballRect7.right >= ballRect12.left &&
                    ballRect7.left <= ballRect12.right &&
                    ballRect7.bottom >= ballRect12.top &&
                    ballRect7.top <= ballRect12.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect7.right >= ballRect13.left &&
                    ballRect7.left <= ballRect13.right &&
                    ballRect7.bottom >= ballRect13.top &&
                    ballRect7.top <= ballRect13.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect7.right >= ballRect14.left &&
                    ballRect7.left <= ballRect14.right &&
                    ballRect7.bottom >= ballRect14.top &&
                    ballRect7.top <= ballRect14.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect7.right >= ballRect15.left &&
                    ballRect7.left <= ballRect15.right &&
                    ballRect7.bottom >= ballRect15.top &&
                    ballRect7.top <= ballRect15.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect7.right >= eightRect.left &&
                    ballRect7.left <= eightRect.right &&
                    ballRect7.bottom >= eightRect.top &&
                    ballRect7.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                //////////////////////////////////////////////////////////9

                if (ballRect9.right >= ballRect10.left &&
                    ballRect9.left <= ballRect10.right &&
                    ballRect9.bottom >= ballRect10.top &&
                    ballRect9.top <= ballRect10.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                    }
                    if (ballRect9.right >= ballRect11.left &&
                        ballRect9.left <= ballRect11.right &&
                        ballRect9.bottom >= ballRect11.top &&
                        ballRect9.top <= ballRect11.bottom) {
                        // برخورد اتفاق افتاده است، تغییر رنگ توپ
                        angle = 200 -angle;
                    }
                if (ballRect9.right >= ballRect12.left &&
                    ballRect9.left <= ballRect12.right &&
                    ballRect9.bottom >= ballRect12.top &&
                    ballRect9.top <= ballRect12.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect9.right >= ballRect13.left &&
                    ballRect9.left <= ballRect13.right &&
                    ballRect9.bottom >= ballRect13.top &&
                    ballRect9.top <= ballRect13.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect9.right >= ballRect14.left &&
                    ballRect9.left <= ballRect14.right &&
                    ballRect9.bottom >= ballRect14.top &&
                    ballRect9.top <= ballRect14.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect9.right >= ballRect15.left &&
                    ballRect9.left <= ballRect15.right &&
                    ballRect9.bottom >= ballRect15.top &&
                    ballRect9.top <= ballRect15.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect9.right >= eightRect.left &&
                    ballRect9.left <= eightRect.right &&
                    ballRect9.bottom >= eightRect.top &&
                    ballRect9.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                ////////////////////////////////////////////////////////////10

                }
                if (ballRect10.right >= ballRect11.left &&
                    ballRect10.left <= ballRect11.right &&
                    ballRect10.bottom >= ballRect11.top &&
                    ballRect10.top <= ballRect11.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect10.right >= ballRect12.left &&
                    ballRect10.left <= ballRect12.right &&
                    ballRect10.bottom >= ballRect12.top &&
                    ballRect10.top <= ballRect12.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect10.right >= ballRect13.left &&
                    ballRect10.left <= ballRect13.right &&
                    ballRect10.bottom >= ballRect13.top &&
                    ballRect10.top <= ballRect13.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect10.right >= ballRect14.left &&
                    ballRect10.left <= ballRect14.right &&
                    ballRect10.bottom >= ballRect14.top &&
                    ballRect10.top <= ballRect14.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect10.right >= ballRect15.left &&
                    ballRect10.left <= ballRect15.right &&
                    ballRect10.bottom >= ballRect15.top &&
                    ballRect10.top <= ballRect15.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect10.right >= eightRect.left &&
                    ballRect10.left <= eightRect.right &&
                    ballRect10.bottom >= eightRect.top &&
                    ballRect10.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                ///////////////////////////////////////////////////////11

                if (ballRect11.right >= ballRect12.left &&
                    ballRect11.left <= ballRect12.right &&
                    ballRect11.bottom >= ballRect12.top &&
                    ballRect11.top <= ballRect12.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect11.right >= ballRect13.left &&
                    ballRect11.left <= ballRect13.right &&
                    ballRect11.bottom >= ballRect13.top &&
                    ballRect11.top <= ballRect13.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect11.right >= ballRect14.left &&
                    ballRect11.left <= ballRect14.right &&
                    ballRect11.bottom >= ballRect14.top &&
                    ballRect11.top <= ballRect14.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect11.right >= ballRect15.left &&
                    ballRect11.left <= ballRect15.right &&
                    ballRect11.bottom >= ballRect15.top &&
                    ballRect11.top <= ballRect15.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect11.right >= eightRect.left &&
                    ballRect11.left <= eightRect.right &&
                    ballRect11.bottom >= eightRect.top &&
                    ballRect11.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                ///////////////////////////////////////////////////////////////12

                if (ballRect12.right >= ballRect13.left &&
                    ballRect12.left <= ballRect13.right &&
                    ballRect12.bottom >= ballRect13.top &&
                    ballRect12.top <= ballRect13.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect12.right >= ballRect14.left &&
                    ballRect12.left <= ballRect14.right &&
                    ballRect12.bottom >= ballRect14.top &&
                    ballRect12.top <= ballRect14.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect12.right >= ballRect15.left &&
                    ballRect12.left <= ballRect15.right &&
                    ballRect12.bottom >= ballRect15.top &&
                    ballRect12.top <= ballRect15.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect12.right >= eightRect.left &&
                    ballRect12.left <= eightRect.right &&
                    ballRect12.bottom >= eightRect.top &&
                    ballRect12.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                /////////////////////////////////////////////////////////13

                if (ballRect13.right >= ballRect14.left &&
                    ballRect13.left <= ballRect14.right &&
                    ballRect13.bottom >= ballRect14.top &&
                    ballRect13.top <= ballRect14.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect13.right >= ballRect15.left &&
                    ballRect13.left <= ballRect15.right &&
                    ballRect13.bottom >= ballRect15.top &&
                    ballRect13.top <= ballRect15.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect13.right >= eightRect.left &&
                    ballRect13.left <= eightRect.right &&
                    ballRect13.bottom >= eightRect.top &&
                    ballRect13.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                //////////////////////////////////////////////////////////////////14

                if (ballRect14.right >= ballRect15.left &&
                    ballRect14.left <= ballRect15.right &&
                    ballRect14.bottom >= ballRect15.top &&
                    ballRect14.top <= ballRect15.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                if (ballRect14.right >= eightRect.left &&
                    ballRect14.left <= eightRect.right &&
                    ballRect14.bottom >= eightRect.top &&
                    ballRect14.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }
                ////////////////////////////////////////////////////////////15

                if (ballRect15.right >= eightRect.left &&
                    ballRect15.left <= eightRect.right &&
                    ballRect15.bottom >= eightRect.top &&
                    ballRect15.top <= eightRect.bottom) {
                    // برخورد اتفاق افتاده است، تغییر رنگ توپ
                    angle = 200 -angle;
                }

                /////////////////////////////////////////////////////////////
            }

        })
            //    // بررسی برخورد با خط
            //    if (collision1(bwhite, wall1)) {
            //        // برخورد با خط، تغییر زاویه به عکس
            //        angle = 180 -angle;
            //    }
            // if (collision2(bwhite, wall2)) {
            //        // برخورد با خط، تغییر زاویه به عکس
            //        angle =180 -angle;

            //    }
            //    if (collision3(bwhite, wall3)) {
            //        // برخورد با خط، تغییر زاویه به عکس
            //        angle = 180 -angle;



            //    }
            //   if (collision4(bwhite, wall4)) {
            //        // برخورد با خط، تغییر زاویه به عکس
            //        angle = 180 -angle;


            //    }
            //    if (collision5(bwhite, wall5)) {
            //        // برخورد با خط، تغییر زاویه به عکس
            //        angle = 180 -angle;

            //        console.log(angle);
            //    }
            //   if (collision6(bwhite, wall6)) {
            //        // برخورد با خط، تغییر زاویه به عکس
            //        angle = 180 -angle;

            //    }
            //    // بازخوانی تابع برای ادامه حرکت
            //    requestAnimationFrame(moveBall);
            //}

            //// تابع بررسی برخورد توپ با خط
            //function collision1($div1, $div2) {
            //    var x1 = $div1.offset().left;
            //    var y1 = $div1.offset().top;
            //    var h1 = $div1.outerHeight(true);
            //    var w1 = $div1.outerWidth(true);
            //    var b1 = y1 + h1;
            //    var r1 = x1 + w1;

            //    var x2 = $div2.offset().left;
            //    var y2 = $div2.offset().top;
            //    var h2 = $div2.outerHeight(true);
            //    var w2 = $div2.outerWidth(true);
            //    var b2 = y2 + h2;
            //    var r2 = x2 + w2;




            //    return !(b1 < y2 || y1 > b2 || r1 < x2 || x1 > r2);
            //}
            //// تابع بررسی برخورد توپ با خط

            //function collision2($div1, $div3) {
            //    var x1 = $div1.offset().left;
            //    var y1 = $div1.offset().top;
            //    var h1 = $div1.outerHeight(true);
            //    var w1 = $div1.outerWidth(true);
            //    var b1 = y1 + h1;
            //    var r1 = x1 + w1;

            //    var x3 = $div3.offset().left;
            //    var y3 = $div3.offset().top;
            //    var h3 = $div3.outerHeight(true);
            //    var w3 = $div3.outerWidth(true);
            //    var b3 = y3 + h3;
            //    var r3 = x3 + w3;

            //    return !(b1 < y3 || y1 > b3 || r1 < x3 || x1 > r3);
            //}

            //// تابع بررسی برخورد توپ با خط
            //function collision3($div1, $div4) {
            //    var x1 = $div1.offset().left;
            //    var y1 = $div1.offset().top;
            //    var h1 = $div1.outerHeight(true);
            //    var w1 = $div1.outerWidth(true);
            //    var b1 = y1 + h1;
            //    var r1 = x1 + w1;

            //    var x4 = $div4.offset().left;
            //    var y4 = $div4.offset().top;
            //    var h4 = $div4.outerHeight(true);
            //    var w4 = $div4.outerWidth(true);
            //    var b4 = y4 + h4;
            //    var r4 = x4 + w4;

            //    return !(b1 < y4 || y1 > b4 || r1 < x4 || x1 > r4);
            //}

            //// تابع بررسی برخورد توپ با خط
            //function collision4($div1, $div5) {
            //    var x1 = $div1.offset().left;
            //    var y1 = $div1.offset().top;
            //    var h1 = $div1.outerHeight(true);
            //    var w1 = $div1.outerWidth(true);
            //    var b1 = y1 + h1;
            //    var r1 = x1 + w1;

            //    var x5 = $div5.offset().left;
            //    var y5 = $div5.offset().top;
            //    var h5 = $div5.outerHeight(true);
            //    var w5 = $div5.outerWidth(true);
            //    var b5 = y5 + h5;
            //    var r5 = x5 + w5;

            //    return !(b1 < y5 || y1 > b5 || r1 < x5 || x1 > r5);
            //}

            //// تابع بررسی برخورد توپ با خط
            //function collision5($div1, $div6) {
            //    var x1 = $div1.offset().left;
            //    var y1 = $div1.offset().top;
            //    var h1 = $div1.outerHeight(true);
            //    var w1 = $div1.outerWidth(true);
            //    var b1 = y1 + h1;
            //    var r1 = x1 + w1;

            //    var x6 = $div6.offset().left;
            //    var y6 = $div6.offset().top;
            //    var h6 = $div6.outerHeight(true);
            //    var w6 = $div6.outerWidth(true);
            //    var b6 = y6 + h6;
            //    var r6 = x6 + w6;

            //    return !(b1 < y6 || y1 > b6 || r1 < x6 || x1 > r6);
            //}

            //// تابع بررسی برخورد توپ با خط
            //function collision6($div1, $div7) {
            //    var x1 = $div1.offset().left;
            //    var y1 = $div1.offset().top;
            //    var h1 = $div1.outerHeight(true);
            //    var w1 = $div1.outerWidth(true);
            //    var b1 = y1 + h1;
            //    var r1 = x1 + w1;


            //    var x7 = $div7.offset().left;
            //    var y7 = $div7.offset().top;
            //    var h7 = $div7.outerHeight(true);
            //    var w7 = $div7.outerWidth(true);
            //    var b7 = y7 + h7;
            //    var r7 = x7 + w7;


            //    return !(b1 < y7 || y1 > b7 || r1 < x7 || x1 > r7);
            //}


            //// شروع حرکت توپ
            //moveBall();
            ///////////////////////////////////////////////////////////
            // A function to calculate the angle of reflection
            //function reflectAngle(incidentAngle, lineAngle) {
            //    // Convert the angles to radians
            //    incidentAngle = incidentAngle * Math.PI / 180;
            //    lineAngle = lineAngle * Math.PI / 180;
            //    // Calculate the normal angle
            //    let normalAngle = lineAngle + Math.PI / 2;
            //    // Calculate the angle of reflection using the formula
            //    // r = i - 2 * (i - n)
            //    let reflectionAngle = incidentAngle - 2 * (incidentAngle - normalAngle);
            //    // Convert the angle to degrees
            //    reflectionAngle = reflectionAngle * 180 / Math.PI;
            //    // Return the angle of reflection
            //    return reflectionAngle;
            //}

            //// A function to create the animation of the ball
            //function animateBall() {
            //    // Get the initial position and velocity of the ball
            //    let x = parseInt($("#dchob").css("left"));
            //    let y = parseInt($("#dchob").css("top"));
            //    let vx = 5; // The initial horizontal velocity in pixels per frame
            //    let vy = 0; // The initial vertical velocity in pixels per frame
            //    // Get the angle and length of the line
            //    let lineAngle = angel; // The angle of the line in degrees
            //    let lineLength = 300; // The length of the line in pixels
            //    // Get the coefficient of restitution of the ball
            //    let e = 0.8; // The ratio of the final to initial relative velocity
            //    // Get the gravity constant
            //    let g = 0.2; // The acceleration due to gravity in pixels per frame squared
            //    // Get the dimensions of the window
            //    let windowWidth = window.innerWidth;
            //    let windowHeight = window.innerHeight;
            //    // Create a loop to update the position and velocity of the ball
            //    let interval = setInterval(function () {
            //        // Update the position of the ball
            //        x += vx;
            //        y += vy;
            //        // Update the velocity of the ball
            //        vy += g;
            //        // Check if the ball hits the line
            //        if (y + 25 >= 200 - (x - 100) * Math.tan(lineAngle * Math.PI / 180) && x >= 100 && x <= 100 + lineLength) {
            //            // Calculate the angle of incidence
            //            let incidentAngle = Math.atan2(vy, vx) * 180 / Math.PI;
            //            // Calculate the angle of reflection
            //            let reflectionAngle = reflectAngle(incidentAngle, lineAngle);
            //            // Calculate the speed of the ball
            //            let speed = Math.sqrt(vx * vx + vy * vy);
            //            // Update the velocity of the ball
            //            vx = speed * Math.cos(reflectionAngle * Math.PI / 180) * e;
            //            vy = speed * Math.sin(reflectionAngle * Math.PI / 180) * e;
            //        }
            //        // Check if the ball hits the bottom or top of the window
            //        if (y + 50 >= windowHeight || y <= 0) {
            //            // Reverse the vertical velocity and reduce it by the coefficient of restitution
            //            vy = -vy * e;
            //        }
            //        // Check if the ball hits the left or right of the window
            //        if (x + 50 >= windowWidth || x <= 0) {
            //            // Reverse the horizontal velocity and reduce it by the coefficient of restitution
            //            vx = -vx * e;
            //        }
            //        // Set the position of the ball
            //        $("#dchob").css("left", x + "px");
            //        $("#dchob").css("top", y + "px");
            //        // Stop the animation if the speed is very low
            //        if (Math.abs(vx) < 0.1 && Math.abs(vy) < 0.1) {
            //            clearInterval(interval);
            //        }
            //    }, 20); // The time interval between each frame in milliseconds
            //}

            //// Call the function to start the animation
            //animateBall();
            ////////////////////////////////////////////////////////////

            //////////////////////////////////////////////////////////
       //        angel = -(angle + 40);
       ///*     $("#echob").css({ 'transform': `rotate(${angle}deg)` });*/
          //  isMouseDown = false;
          //  if (isMouseDown == false) {
          //      $("#echob").hide();

          //      var x = $("#dchob").position().left; // این قسمت برای گرفتن موقعیت افقی توپ قرمز است
          //      var y = $("#dchob").position().top; // این قسمت برای گرفتن موقعیت عمودی توپ قرمز است

          //      //var angle = Math.atan2(y - 50, x - 10); // این قسمت برای محاسبه زاویه حرکت توپ قرمز است
          //      //var distance = Math.sqrt((y - 50) * (y - 50) + (x - 10) * (x - 10)); // این قسمت برای محاسبه فاصله توپ قرمز از نقطه شروع است
          //      var speed = 100 / 10; // این قسمت برای محاسبه سرعت توپ قرمز است
          //      var vx = Math.cos(angle); // این قسمت برای محاسبه سرعت افقی توپ قرمز است
          //      var vy = Math.sin(angle); // این قسمت برای محاسبه سرعت عمودی توپ قرمز است
          //      $("#dchob").animate({ left: "+=" + vx * 100, top: "+=" + vy * 100 }, 1000); // این قسمت برای انیمیشن توپ قرمز است که باعث می شود توپ قرمز به سمتی که رها شده است، حرکت کند در طول یک ثانیه
          //}



                document.addEventListener("mousemove", function (e) {

                    if (isMouseDown == true) {
                      /*  $(".ewhite").css({ 'top': '0px' });*/
                        $("#echob").show();

                        const currentX = e.clientX;
                        const currentY = e.clientY;
                        const deltaX = currentX - initialX;
                        const deltaY = currentY - initialY;
                        angle = Math.atan2(deltaY, deltaX) * 180 / Math.PI;

                        element.style.transform = `rotate(${angle}deg)`;


                    }
                });





        function push()
        {

        }


          function eeight(deg,speed)
        {

        }

         $(".ebal1").click(function()
        {

        })
          $(".ebal2").click(function()
        {

        })
          $(".ebal3").click(function()
        {

        })
          $(".ebal4").click(function()
        {

        })
          $(".ebal5").click(function()
        {

        })
          $(".ebal6").click(function()
        {

        })
          $(".ebal7").click(function()
        {

        })
          $(".eeight").click(function()
        {

        })
          $(".ebal9").click(function()
        {

        })
          $(".ebal10").click(function()
        {

        })
          $(".ebal11").click(function()
        {

        })
          $(".ebal12").click(function()
        {

        })
          $(".ebal13").click(function()
        {

        })
          $(".ebal14").click(function()
        {

        })
          $(".ebal15").click(function()
        {

        })
        function b1(deg,speed)
        {

        }
        function b2(deg,speed)
        {

        }
        function b3(deg,speed)
        {

        }
        function b4(deg,speed)
        {

        }
        function b5(deg,speed)
        {

        }
        function b6(deg,speed)
        {

        }
        function b7(deg,speed)
        {

        }
        function b9(deg,speed)
        {

        }
        function b10(deg,speed)
        {

        }
        function b11(deg,speed)
        {

        }
        function b12(deg,speed)
        {

        }
        function b13(deg,speed)
        {

        }
        function b14(deg,speed)
        {

        }
        function b15(deg,speed)
        {

        }
        function ballstart(){
            $("#dchob").css({ 'top': '350px', 'left': '-200px' });
            $(".ewhite").css({ 'top': '7px' });

            //$(".echob").css({ 'top': '160px', 'left': '30px' });


        $(".ebal1").css({ 'top': '150px','left':'-250px' });
            $(".ebal2").css({ 'top': '178px','left':'-271px' });
            $(".ebal3").css({ 'top': '116px','left':'-234px' });
            $(".ebal4").css({ 'top': '162px','left':'-229px' });
            $(".ebal5").css({ 'top': '207px','left':'-222px' });
            $(".ebal6").css({ 'top': '146px','left':'-188px' });
            $(".ebal7").css({ 'top': '86px','left':'-152px' });
            $(".ebal9").css({ 'top': '165px','left':'-37px' });
            $(".ebal10").css({ 'top': '134px','left':'-2px' });
            $(".ebal11").css({ 'top': '193px','left':'-23px' });
            $(".ebal12").css({ 'top': '131px','left':'12px' });
            $(".ebal13").css({ 'top': '100px','left':'47px' });
            $(".ebal14").css({ 'top': '176px','left':'52px' });
            $(".ebal15").css({ 'top': '116px','left':'87px' });
            $(".eeight").css({ 'top': '147px','left':'-340px' });

        }
    </script>
     
